# Traceability Matrix

The whole-project map from **spec → code**. These matrices are the *instance* of a broader **bidirectional traceability graph** — business objective → feature → use case / FR → design → code → tests, with business rules and quality attributes as cross-cutting layers — described as one picture in [methodology.md, *The traceability model*](methodology.md#the-traceability-model). This file is where that model is recorded and verified — on two primary realization axes (functional and non-functional), plus a cross-cutting coverage layer for the off-spine requirement types:

- **[Functional traceability](#functional-traceability-matrix)** — one row per use case (`UC-<AREA>-<slug>`), linking it to the cross-cutting functional requirements it depends on and the actual frontend/backend/test artifacts that implement it. This is how Claude (and you) find the right code from a requirement, and — via the separate **Build** and **Verify** columns — see at a glance **which use cases are built and which of those are actually test-verified**. The two states are tracked independently on purpose: a built-but-untested use case stays visibly unverified instead of being absorbed into a single "done" symbol. Its companion, the **[non-use-case FR matrix](#non-use-case-fr-traceability)**, gives each cross-cutting `FR-<AREA>-*` its own realization row — because a non-use-case FR is a *functional* requirement node in its own right (sibling of the use case), not just a dependency annotation on UC rows.
- **[Non-functional traceability](#non-functional-traceability-matrix)** — one row per quality attribute (`PER-*`, `SEC-*`, `ROB-*`, …), mapping it to the architecture-of-record's quality scenario (`QS-n`) that operationalizes it and the test that verifies it. Quality attributes trace through scenarios and architecture decisions, **not** through use cases, so they live in their own matrix rather than as UC rows.
- **[Cross-cutting coverage](#bo-measurement-matrix)** — the layers the two realization matrices don't capture. Each business objective (`BO-*`) traces *forward to outcome* through its success metric (`SM-<slug>`) — was the objective **achieved**, not merely built? — in the **BO measurement matrix**. Each risk (`RI-*`) traces forward to the mitigating requirement(s)/decision that responds to it, or is consciously accepted, in the **risk coverage matrix**. (Business rules `BR-*` are the third off-spine layer; they are traced transitively through the use cases / FRs that enforce them and so carry no realization row — see the business-rule coverage check in `/spec-build`.)

Together these make this file the single place to ask "is requirement X traced to code and verified?" for **both** functional and non-functional requirements. Note: in this project a **use case is itself a (high-level) functional requirement**, and its "The system …" steps + Associated Information are its detailed functional spec (the SRS's Use Cases section); the SRS's Non-Use Case Functional Requirements "shall" statements capture only the system-level behaviors. The `QS-n` scenario *definitions* (stimulus/response/measure) are owned by the [architecture-of-record](design/architectural-design.md#quality-requirements) and cited here by ID; this file owns the **verification map** (which test proves each scenario, and its state).

> `/design` sets a use case's **Build** to `📐 Designed` and fills the **Design** column with the area's design-of-record when it is written; `/implement` fills the code columns, sets **Build**, and fills **Verify** at the end of its run (Phase 5).

## How to use it

- **One row per use case**, in `UC-<AREA>-<slug>` order, mirroring `requirements/use-cases.md`.
- **Build** — construction state of the code, from the Build legend below (`❌` → `📐` → `🟡` → `✅`, or `🚫` for tabled). It says nothing about tests; `✅ Built` is a claim about code only.
- **Verify** — whether automated tests exercise and pass for the built behavior, from the Verify legend below. Deliberately **independent of Build**: a `✅ Built` use case with no test reads `🔎 None` here, so "done" can never hide an untested flow. `—` until there is built behavior to verify (Build is `❌`/`📐`/`🚫`). RAM has no frontend automated tests yet (see the matrix note), so a built RAM use case's Verify reflects its **backend** test coverage; that frontend-test gap is recorded once rather than per row.
- **FR IDs** — the SRS's non-use-case, system-level functional requirements this use case **depends on / builds upon** (e.g. autosave, validation, real-time collaboration, AI), and, where relevant, the External Interface Requirements (`SI-*`) that carry the use case's interface detail (export formats/fidelity, import allowlist/extraction). The use case's own "The system …" steps are its primary acceptance criteria; this column points to the reusable cross-cutting subsystems behind it. **This column is deliberately non-exhaustive — read it as additive over a universal baseline, not as the complete FR set for the row.** The most universal behaviors — RBAC (`FR-SEC-*`) and authorship metadata (`FR-HIS-authorship-metadata`) — apply to **nearly every** row, so they are an **implicit baseline omitted from every cell** to reduce noise: their absence from a row is **never informative** — it never means the use case escapes authorization or authorship. The column therefore lists only the *additional* cross-cutting FRs a use case draws on **beyond** that baseline, and **`—` means "none beyond the baseline"** (the use case is otherwise self-contained — its own steps are the whole spec), **not** "this use case has no security or authorship dimension." Section locking is part of the editing use cases' own steps (governed by `BR-edit-lock-required` / `BR-lock-expiry`), not a separate FR.
- **Design** — the area's design-of-record (`design/<area>.md`, optionally anchored to the use case's subsection) once `/design` has written it; `—` until then. A single area doc covers every use case in its area, so rows in the same area cite the same file.
- **Frontend / Backend / Tests** — concrete artifacts (`file_path` or component/class names) once built; `—` until then.
- **Extension coverage (convention).** Because tests are tagged to the **whole use case** — there are no per-step or per-extension IDs — the **Tests** cell names *which extensions* a test exercises, e.g. `…AddEvaluation… + matching extensions (E1,E3); E2 untested`. This keeps edge-case coverage visible: a `✅ Verified` row whose negative/exception flows are only partly tested shows `🟡 Partial` in Verify with the gap spelled out in Tests. `/sync-check` flags any `✅ Built` use case whose count of negative-path tests is below its number of extensions (a heuristic that surfaces likely-undertested exception behavior). Back-filling the per-extension annotation across the already-built use cases is a `/sync-check` sweep, not a precondition for the convention.
- When a use case is added to `requirements/use-cases.md`, add its row here (Build `❌ Not started`, Verify `—`). `/design` advances Build to `📐 Designed`; when `/implement` finishes it, set Build, fill Verify, and fill the code columns.

## Build legend

| Symbol | Meaning |
|--------|---------|
| ❌ Not started | No code yet. |
| 📐 Designed | Design-of-record exists — the area's `design/<area>.md` is written and approved (`/design`); not yet coded. |
| 🟡 In progress | Partially built (e.g., backend complete, frontend pending). |
| ✅ Built | Code complete for the use case's flow. **Makes no claim about tests** — see the Verify column. |
| 🚫 Tabled | Deliberately out of MVP scope (e.g., UC-ART-promote-selection). Not a gap to fill. |

## Verify legend

| Symbol | Meaning |
|--------|---------|
| ✅ Verified | Automated test(s) exercise the built behavior — main flow and extensions — and pass. |
| 🟡 Partial | Some flows/extensions are tested; others are not (the Tests cell names the gap). |
| 🔎 None | Built, but no (or insufficient) automated test exists. |
| — | Nothing built to verify yet (Build is `❌`, `📐`, or `🚫`). |

## Feature → use case area coverage

`requirements/vision-and-scope.md` lists product **features** (stakeholder-visible capabilities); `requirements/use-cases.md` groups behavioral specs into **use case areas** (`UC-<AREA>-*`). The relationship is many-to-many — one feature usually decomposes into use cases across several areas, and one area can serve more than one feature. This map keeps them aligned without making `vision-and-scope.md` cite UC IDs directly. See [methodology.md](methodology.md), *Features and use case areas — different views, not different fragments*, for why the two are not made 1:1.

The first column lists each feature by its `FEAT-<slug>` ID and name, exactly as defined in `vision-and-scope.md`; the map is **keyed to the ID**, and `/spec-build`'s feature-coverage check verifies the two sets match (every `FEAT-*` has exactly one row here, and every row resolves to a defined feature). The **BOs** column records each feature's **business objectives**; and the last column names every UC area whose use cases realize that feature, with the area that owns the primary surface listed first.

The **BOs** column is the coarse, vision-level `BO → feature` edge — *why does this capability exist?* — and is deliberately **distinct from** the fine, realization-level `BO → use case / FR` edge carried by the **Supports BOs** column in the functional matrix (a use case *advances* an objective; a feature *exists to serve* it). They are two edges of the same `BO → feature → use case` chain, so a feature may name an objective that **none of its individual UC rows carry** — e.g. `BO-PERF-submission-rate` / `BO-PERF-faster-completion`, realized through the cross-cutting `FR-NOT-weekly-reminder` rather than any single use case. This column is a vision aid, **not** a coverage realizer: `/spec-build`'s forward BO check still reads the UC-level `Supports BOs` column and the SRS's `_(Supports BO-…)_` FR annotations, never this one. Enabling/substrate features that serve no measurable objective directly show `—`; `BO-RAM-research` appears in no row, being the platform-level emergent objective with no single-feature realizer.

| Feature | BOs | UC areas |
|---|---|---|
| FEAT-administration: Administration and Course Management | — (enabling/substrate) | SEC, TEA, STU, INS, ACC |
| FEAT-rubric-management: Rubric Management | — (enabling; feeds peer-evaluation grading) | RUB |
| FEAT-weekly-activity-reports: Weekly Activity Reports | BO-PERF-instructor-workload, BO-PERF-submission-rate, BO-PERF-faster-completion | WAR |
| FEAT-peer-evaluations: Peer Evaluations | BO-PERF-faster-grading, BO-PERF-instructor-workload, BO-PERF-submission-rate, BO-PERF-faster-completion | EVA |
| FEAT-template-management: Template Management | BO-RAM-requirement-quality | TPL |
| FEAT-glossary-consistency: Glossary and Terminology Consistency | BO-RAM-consistency | GLO (terms also drawn on by VAL, LNK, AI) |
| FEAT-document-authoring: Document and Use Case Authoring | BO-RAM-learning-outcomes | DOC |
| FEAT-graph-model: Graph-First Requirements Model | BO-RAM-traceability, BO-RAM-navigation | ART (with LNK for typed edges, GLO for safe rename of terms) |
| FEAT-traceability: Full Requirements Traceability | BO-RAM-traceability, BO-RAM-navigation | LNK |
| FEAT-validation: Smart Editing and Validation (ReqLint) | BO-RAM-requirement-quality, BO-RAM-instructor-workload, BO-RAM-consistency | VAL |
| FEAT-ai-assistance: AI-Assisted Guidance and Feedback | BO-RAM-learning-outcomes, BO-RAM-requirement-quality, BO-RAM-instructor-workload | CFG (per-section setup), AI (assistants, incl. project-source-material import) |
| FEAT-collaboration: Collaboration and Document Workflow | BO-RAM-instructor-workload | COL, REV |
| FEAT-instructor-review: Instructor Dashboard, Feedback, and Grading | BO-RAM-instructor-workload | REV |
| FEAT-export: Export and Delivery | — (delivery; no measurable objective directly) | EXP |

## Functional traceability matrix

> Mapped against the repository on 2026-06-20. `Frontend` cites the Vue page(s) + `apis/ram` call(s); `Backend` cites the controller (+ key method) and supporting service/entity; `Tests` cites the backend test class + methods (`—` = none yet). Frontend modules are under `frontend/src/` (`pages/ram/*.vue`, `apis/ram/`); backend modules under `backend/src/main/java/team/projectpulse/ram/`; tests under `backend/src/test/java/team/projectpulse/ram/`. No frontend automated tests exist for RAM yet, so RAM `Verify` states reflect backend coverage only. Note: the glossary endpoints (`GlossaryController`) and the requirement-artifact delete endpoint currently have **no** backend tests — see the `🔎 None` rows.

> **Foundation & performance-tracking rows (`UC-RUB-*`, `UC-SEC-*`, `UC-TEA-*`, `UC-STU-*`, `UC-INS-*`, `UC-ACC-*`, `UC-WAR-*`, `UC-EVA-*`).** These are the established, in-production course-management, weekly-activity-report, and peer-evaluation features. Their concrete spec→code mapping was filled in by **design-as-built code archaeology** (these features were built before this spec set existed): Frontend cites the Vue page(s) under `frontend/src/pages/` + the `apis/<feature>` call(s); Backend cites the controller (+ key method) → service → repository/entity under `backend/src/main/java/team/projectpulse/{course,section,team,student,instructor,activity,evaluation,rubric}`; Tests cites the backend unit (`*ServiceTest`) and integration (`*IntegrationTest`) methods. Most are `✅ Built` / `✅ Verified`; several are `🟡 In progress` where the code shows a feature only partly wired (`UC-TEA-delete-team` delete-team UI + integration test commented out; `UC-INS-deactivate-instructor`/`UC-INS-reactivate-instructor` and `UC-STU-deactivate-student`/`UC-STU-reactivate-student` deactivate/reactivate have no dedicated endpoint, UI control, or targeted test — only the generic `updateInstructor`/`updateStudent` `enabled`-flag path, hence `🔎 None` in Verify). There are **no frontend automated tests** for these features either.

| UC | Use Case | Build | Verify | FR IDs | Supports BOs | Design | Frontend | Backend | Tests |
|----|----------|-------|--------|--------|--------------|--------|----------|---------|-------|
| UC-RUB-create-rubric | The Course Admin creates a rubric | ✅ Built | ✅ Verified | — | — | — | `Rubrics.vue` → `apis/rubric` `createRubric` | `RubricController.addRubric → RubricService.saveRubric → RubricRepository` / `Rubric` | `RubricServiceTest.testSaveRubric`, `RubricIntegrationTest.addRubric` |
| UC-RUB-find-rubrics | The Course Admin finds rubrics | ✅ Built | ✅ Verified | — | — | — | `Rubrics.vue` → `apis/rubric` `searchRubrics` | `RubricController.findRubricsByCriteria → RubricService.findByCriteria` (+ `RubricSpecs`) / `Rubric` | `RubricServiceTest.testFindByCriteria`; `RubricIntegrationTest.adminBingyangFindRubricsByCriteria`, `adminTimFindRubricsByCriteria` |
| UC-RUB-view-rubric | The Course Admin views a rubric | ✅ Built | ✅ Verified | — | — | — | `Rubrics.vue` → `apis/rubric` `findRubricById` | `RubricController.findRubricById → RubricService.findRubricById` / `Rubric` | `RubricServiceTest.testFindRubricById`; `RubricIntegrationTest.adminBingyangFindRubricById`, `adminTimFindRubricById`, `studentJohnFindRubricById`, `studentJanaFindRubricById` |
| UC-RUB-edit-rubric | The Course Admin edits a rubric | ✅ Built | ✅ Verified | — | — | — | `Rubrics.vue` → `apis/rubric` `updateRubric` | `RubricController.updateRubric → RubricService.updateRubric` / `Rubric` | `RubricServiceTest.testUpdateRubric`; `RubricIntegrationTest.adminBingyangUpdateRubric`, `adminTimUpdateRubric` |
| UC-RUB-delete-rubric | The Course Admin deletes a rubric | 🟡 In progress | 🟡 Partial | — | — | — | — (delete control commented out in `Rubrics.vue`; no `deleteRubric` API) | `RubricController.deleteRubric → RubricService.deleteRubric` / `Rubric` | `RubricServiceTest.testDeleteRubric` (integration `RubricIntegrationTest.deleteRubric` commented out) |
| UC-RUB-add-criterion | The Course Admin adds a criterion to a rubric | ✅ Built | ✅ Verified | — | — | — | `Rubrics.vue` → `apis/rubric` `assignCriterionToRubric` | `RubricController.assignCriterionToRubric → RubricService.addCriterionToRubric` / `Rubric` | `RubricServiceTest.testAddCriterionToRubric`; `RubricIntegrationTest.assignCriterionToRubric`, `assignCriterionToRubricNotInSameCourse` |
| UC-RUB-remove-criterion | The Course Admin removes a criterion from a rubric | ✅ Built | ✅ Verified | — | — | — | `Rubrics.vue` → `apis/rubric` `unassignCriterionFromRubric` | `RubricController.removeCriterionFromRubric → RubricService.removeCriterionFromRubric` / `Rubric` | `RubricServiceTest.testRemoveCriterionFromRubric`; `RubricIntegrationTest.removeCriterionFromRubric`, `removeCriterionFromRubricNotInSameCourse` |
| UC-RUB-assign-rubric | The Course Admin assigns a rubric to a course section | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `assignRubricToSection` | `SectionController.assignRubric → SectionService.assignRubric` / `Section` | `SectionServiceTest.testAssignRubric`; `SectionIntegrationTest.adminBingyangAssignRubric`, `adminTimAssignRubric` |
| UC-RUB-find-criteria | The Course Admin finds criteria | ✅ Built | ✅ Verified | — | — | — | `Criteria.vue` → `apis/rubric` `searchEvaluationCriteria` | `CriterionController.findPeerEvaluationCriteriaByCriteria → CriterionService.findByCriteria` (+ `CriterionSpecs`) / `Criterion` | `CriterionServiceTest.testFindByCriteria` |
| UC-RUB-view-criterion | The Course Admin views a criterion | ✅ Built | ✅ Verified | — | — | — | `Criteria.vue` → `apis/rubric` `findCriterionById` | `CriterionController.findPeerEvaluationCriterionById → CriterionService.findCriterionById` / `Criterion` | `CriterionServiceTest.testFindCriterionById`; `CriterionIntegrationTest.adminBingyangFindCriterionById`, `adminTimFindCriterionById` |
| UC-RUB-create-criterion | The Course Admin creates a criterion | ✅ Built | ✅ Verified | — | — | — | `Criteria.vue` → `apis/rubric` `createCriterion` | `CriterionController.addCriterion → CriterionService.saveCriterion` / `Criterion` | `CriterionServiceTest.testSaveCriterion`; `CriterionIntegrationTest.adminBingyangAddCriterion` |
| UC-RUB-edit-criterion | The Course Admin edits a criterion | ✅ Built | ✅ Verified | — | — | — | `Criteria.vue` → `apis/rubric` `updateCriterion` | `CriterionController.updateCriterion → CriterionService.updateCriterion` / `Criterion` | `CriterionServiceTest.testUpdateCriterion`; `CriterionIntegrationTest.adminBingyangUpdateCriterion`, `adminTimUpdateCriterion` |
| UC-RUB-delete-criterion | The Course Admin deletes a criterion | 🟡 In progress | 🟡 Partial | — | — | — | — (delete control commented out in `Criteria.vue`; no `deleteCriterion` API) | `CriterionController.deleteCriterion → CriterionService.deleteCriterion` / `Criterion` | `CriterionServiceTest.testDeleteCriterion` (integration `CriterionIntegrationTest.deleteCriterion` commented out) |
| UC-SEC-find-sections | The Course Admin finds course sections | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `searchSections` | `SectionController.findSectionsByCriteria → SectionService.findByCriteria` (+ `SectionSpecs`) / `Section` | `SectionServiceTest.testFindByCriteria`; `SectionIntegrationTest.adminBingyangFindSectionsByCriteria`, `instructorBillFindSectionsByCriteria`, `adminTimFindSectionsByCriteria` |
| UC-SEC-view-section | The Course Admin views a course section | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `findSectionById` | `SectionController.findSectionById → SectionService.findSectionById` / `Section` | `SectionServiceTest.testFindSectionById`; `SectionIntegrationTest.adminBingyangFindSectionById`, `studentJohnFindSectionById`, `adminTimFindSectionById` |
| UC-SEC-create-section | The Course Admin creates a course section | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `createSection` | `SectionController.addSection → SectionService.saveSection` / `Section` | `SectionServiceTest.testSaveSection`; `SectionIntegrationTest.adminBingyangAddSection`, `instructorBillAddSection` |
| UC-SEC-edit-section | The Course Admin edits a course section | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `updateSection` | `SectionController.updateSection → SectionService.updateSection` / `Section` | `SectionServiceTest.testUpdateSection`; `SectionIntegrationTest.adminBingyangUpdateSection`, `adminTimUpdateSection` |
| UC-SEC-setup-active-weeks | The Course Admin sets up active weeks for a course section | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `setUpActiveWeeks` | `SectionController.setUpActiveWeeks → SectionService.setUpActiveWeeks` / `Section` | `SectionServiceTest.testSetUpActiveWeeks`; `SectionIntegrationTest.adminBingyangSetUpActiveWeeks`, `instructorBillSetUpActiveWeeks`, `adminTimSetUpActiveWeeks` |
| UC-TEA-find-teams | The Course Admin/Instructor finds teams | ✅ Built | ✅ Verified | — | — | — | `Teams.vue` → `apis/team` `searchTeams` | `TeamController.findTeamsByCriteria → TeamService.findByCriteria` (+ `TeamSpecs`) / `Team` | `TeamServiceTest.testFindByCriteria`; `TeamIntegrationTest.adminBingyangFindTeamsByCriteria`, `adminTimFindTeamsByCriteria` |
| UC-TEA-view-team | The Course Admin/Instructor views a team | ✅ Built | ✅ Verified | — | — | — | `Teams.vue` → `apis/team` `findTeamById` | `TeamController.findTeamById → TeamService.findTeamById` / `Team` | `TeamServiceTest.testFindTeamById`; `TeamIntegrationTest.adminBingyangFindTeamById`, `instructorBillFindTeamById`, `studentJohnFindTeamById` |
| UC-TEA-create-team | The Course Admin creates a team | ✅ Built | ✅ Verified | — | — | — | `Teams.vue` → `apis/team` `createTeam` | `TeamController.addTeam → TeamService.saveTeam` / `Team` | `TeamServiceTest.testSaveTeam`; `TeamIntegrationTest.adminBingyangAddTeam`, `instructorBillAddTeam` |
| UC-TEA-edit-team | The Course Admin edits a team | ✅ Built | ✅ Verified | — | — | — | `Teams.vue` → `apis/team` `updateTeam` | `TeamController.updateTeam → TeamService.updateTeam` / `Team` | `TeamServiceTest.testUpdateTeam`; `TeamIntegrationTest.adminBingyangUpdateTeam`, `instructorBillUpdateTeam`, `adminTimUpdateTeam` |
| UC-TEA-assign-students | The Course Admin assigns students to teams | ✅ Built | ✅ Verified | — | — | — | `Teams.vue` → `apis/team` `assignStudentToTeam` | `TeamController.assignStudentToTeam → TeamService.assignStudentToTeam` (+ `StudentRepository`) / `Team` | `TeamServiceTest.testAssignStudentToTeam`; `TeamIntegrationTest.assignStudentToTeam`, `assignStudentToTeamNotInSameSection` |
| UC-TEA-remove-student | The Course Admin removes a student from a team | ✅ Built | ✅ Verified | — | — | — | `Teams.vue` → `apis/team` `removeStudentFromTeam` | `TeamController.removeStudentFromTeam → TeamService.removeStudentFromTeam` / `Team` | `TeamServiceTest.testRemoveStudentFromTeam`; `TeamIntegrationTest.removeStudentFromTeam` |
| UC-TEA-delete-team | The Course Admin deletes a team | 🟡 In progress | 🟡 Partial | — | — | — | — (delete control commented out in `Teams.vue`) | `TeamController.deleteTeam → TeamService.deleteTeam → TeamRepository` / `Team` | `TeamServiceTest.testDeleteTeam` (integration `TeamIntegrationTest.deleteTeam` commented out) |
| UC-STU-invite-students | The Course Admin invites students to join a course section | ✅ Built | ✅ Verified | — | — | — | `InviteUsersForm.vue` → `apis/section` `sendEmailInvitationsToStudents` | `SectionController.sendEmailInvitationsToStudents → UserInvitationService.sendEmailInvitations` / `UserInvitation` | `UserInvitationServiceTest.testSendEmailInvitations`; `SectionIntegrationTest.adminBingyangTestSendEmailInvitations`, `adminTimTestSendEmailInvitations` |
| UC-STU-find-students | The Course Admin/Instructor finds students | ✅ Built | ✅ Verified | — | — | — | `Students.vue` → `apis/student` `searchStudents` | `StudentController.findStudentsByCriteria → StudentService.findByCriteria` (+ `StudentSpecs`) / `Student` | `StudentServiceTest.testFindByCriteria`; `StudentIntegrationTest.testFindStudentsByCriteria` |
| UC-STU-view-student | The Course Admin/Instructor views a student | ✅ Built | ✅ Verified | — | — | — | `Students.vue` / `EditStudentForm.vue` → `apis/student` `findStudentById` | `StudentController.findStudentById → StudentService.findStudentById` / `Student` | `StudentServiceTest.testFindStudentById`; `StudentIntegrationTest.testInstructorBillFindStudentInfoById`, `testStudentJohnFindOwnInfoById` |
| UC-STU-delete-student | The Course Admin deletes a student | ✅ Built | ✅ Verified | — | — | — | `Students.vue` → `apis/student` `deleteStudent` | `StudentController.deleteStudent → StudentService.deleteStudent` / `Student` | `StudentServiceTest.testDeleteStudent`; `StudentIntegrationTest.testAdminDeletesStudent`, `testStudentDeletesAnotherStudent` |
| UC-STU-deactivate-student | The Course Admin/Instructor deactivates a student | 🟡 In progress | 🔎 None | — | — | — | — (no dedicated control; via student edit) → `apis/student` `updateStudent` | `StudentController.updateStudent → StudentService.updateStudent` (`enabled` flag; no dedicated deactivate endpoint) / `Student` | — (no targeted deactivate test; only the generic `updateStudent` path) |
| UC-STU-reactivate-student | The Course Admin/Instructor reactivates a student | 🟡 In progress | 🔎 None | — | — | — | — (no dedicated control; via student edit) → `apis/student` `updateStudent` | `StudentController.updateStudent → StudentService.updateStudent` (`enabled` flag; no dedicated reactivate endpoint) / `Student` | — (no targeted reactivate test; only the generic `updateStudent` path) |
| UC-INS-invite-instructors | The Course Admin invites instructors to register an account | ✅ Built | 🔎 None | — | — | — | `InviteUsersForm.vue` → `apis/section` `inviteOrAddInstructors` | `SectionController.inviteOrAddInstructors → UserInvitationService.sendEmailInvitations` / `UserInvitation` | — (no test exercises the `invite-or-add` path) |
| UC-INS-assign-instructors | The Course Admin assigns instructors to teams | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/team` `assignInstructorToTeam` | `TeamController.assignInstructorToTeam → TeamService.assignInstructorToTeam` (+ `InstructorRepository`) / `Team` | `TeamServiceTest.testAssignInstructorToTeam`; `TeamIntegrationTest.assignInstructorToTeam`, `assignInstructorToTeamNotSameSection` |
| UC-INS-remove-instructor | The Course Admin removes an instructor from a team | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/section` `removeInstructorFromSection` | `SectionController.removeInstructor → SectionService.removeInstructor` / `Section` | `SectionServiceTest.testRemoveInstructorSuccess`, `testRemoveInstructorFailsWhenLastInstructor`; `SectionIntegrationTest.adminBingyangRemoveInstructor`, `adminTimRemoveInstructor` |
| UC-INS-find-instructors | The Course Admin finds instructors | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` (no dedicated instructor page) → `apis/instructor` `fetchInstructors` / `apis/section` `getInstructors` | `InstructorController.findInstructorsByCriteria → InstructorService.findByCriteria` (+ `InstructorSpecs`); section-scoped `SectionController.getInstructors` / `Instructor` | `InstructorServiceTest.testFindByCriteria`; `InstructorIntegrationTest.testFindInstructorsByCriteria` |
| UC-INS-view-instructor | The Course Admin views an instructor | ✅ Built | ✅ Verified | — | — | — | `Sections.vue` → `apis/instructor` `findInstructorById` | `InstructorController.findInstructorById → InstructorService.findInstructorById` / `Instructor` | `InstructorServiceTest.testFindInstructorById`; `InstructorIntegrationTest.testAdminBingyangFindInstructorBillById` |
| UC-INS-deactivate-instructor | The Course Admin deactivate an instructor | 🟡 In progress | 🔎 None | — | — | — | — (no dedicated control; via instructor edit) → `apis/instructor` `updateInstructor` | `InstructorController.updateInstructor → InstructorService.updateInstructor` (`enabled` flag; no dedicated deactivate endpoint) / `Instructor` | `InstructorServiceTest.testAdminUpdatesInstructor` (generic update; no targeted deactivate test) |
| UC-INS-reactivate-instructor | The Course Admin reactivate an instructor | 🟡 In progress | 🔎 None | — | — | — | — (no dedicated control; via instructor edit) → `apis/instructor` `updateInstructor` | `InstructorController.updateInstructor → InstructorService.updateInstructor` (`enabled` flag; no dedicated reactivate endpoint) / `Instructor` | `InstructorServiceTest.testAdminUpdatesInstructor` (generic update; no targeted reactivate test) |
| UC-ACC-setup-student-account | The Student sets up a student account | ✅ Built | ✅ Verified | — | — | — | `UserRegistration.vue` → `apis/student` `createStudent` | `StudentController.addStudent → StudentService.saveStudent` (+ `UserInvitationService.validateUserInvitation`) / `Student` | `StudentServiceTest.testSaveStudent`; `StudentIntegrationTest.testAddStudent`, `testAddStudentWithInvalidUserInvitation` |
| UC-ACC-edit-account | The Student edits an account | ✅ Built | ✅ Verified | — | — | — | `UserInfo.vue` → `apis/student` `updateStudent` | `StudentController.updateStudent → StudentService.updateStudent` / `Student` | `StudentServiceTest.testStudentUpdateOwnInfo`, `testInstructorUpdatesStudent`; `StudentIntegrationTest.testStudentJohnUpdatesOwnInfo` |
| UC-ACC-setup-instructor-account | The Instructor sets up an instructor account | ✅ Built | ✅ Verified | — | — | — | `UserRegistration.vue` → `apis/instructor` `createInstructor` | `InstructorController.addInstructor → InstructorService.saveInstructor` (+ `UserInvitationService.validateUserInvitation`) / `Instructor` | `InstructorServiceTest.testSaveInstructor`; `InstructorIntegrationTest.testAddInstructor`, `testAddInstructorWithInvalidUserInvitation` |
| UC-WAR-manage-activities | The Student manages activities in a Weekly Activity Report (WAR) | ✅ Built | ✅ Verified | — | BO-PERF-instructor-workload | — | `MyActivities.vue`, `AddActivityForm.vue`, `EditActivityForm.vue` → `apis/activity` `createActivity`, `updateActivity`, `deleteActivity`, `searchActivities` | `ActivityController.addActivity / updateActivity / deleteActivity / findActivitiesByCriteria → ActivityService.saveActivity / updateActivity / deleteActivity / findByCriteria` / `Activity` | `ActivityServiceTest.testSaveActivity`, `testUpdateActivity`, `testDeleteActivity`, `testFindByCriteria`; `ActivityIntegrationTest.testStudentJohnAddActivity`, `testStudentJohnUpdatesOwnActivity`, `testStudentJohnDeletesOwnActivity` |
| UC-WAR-team-war-report | The Instructor/Student generates a WAR report of a team | ✅ Built | ✅ Verified | — | BO-PERF-instructor-workload | — | `TeamsActivities.vue` → `apis/activity` `searchActivities` (teamId + week) | `ActivityController.findActivitiesByCriteria → ActivityService.findByCriteria` (+ `ActivitySpecs`); report composed client-side / `Activity` | `ActivityServiceTest.testFindByCriteria`; `ActivityIntegrationTest.testAdminBingyangFindActivitiesByCriteria`, `testStudentJohnFindActivitiesByCriteria` |
| UC-WAR-student-war-report | The Instructor generates a WAR report of the student | ✅ Built | 🟡 Partial | — | BO-PERF-instructor-workload | — | `activities/admin/StudentActivities.vue` (+ `SearchPeriod.vue`) → `apis/activity` `searchActivities` (studentId + startWeek/endWeek) | `ActivityController.findActivitiesByCriteria → ActivityService.findByCriteria` (+ `ActivitySpecs` week-range); report composed client-side / `Activity` | `ActivityServiceTest.testFindByCriteria` (no dedicated period integration test) |
| UC-EVA-submit-evaluation | The Student submits a peer evaluation for the previous week | ✅ Built | ✅ Verified | — | BO-PERF-instructor-workload | — | `SubmitTeamsEvaluations.vue` → `apis/evaluation` `submitEvaluation`, `getEvaluationsByEvaluatorIdAndWeek`, `updateEvaluation` | `EvaluationController.addPeerEvaluation → EvaluationService.addPeerEvaluation → PeerEvaluationRepository` / `PeerEvaluation` | `EvaluationServiceTest.testAddPeerEvaluationSuccess`, `…NotInActiveWeeks`, `…NotForPreviousWeek`, `…EvaluatorEvaluateeNotOnTheSameTeam`, `…DuplicatedEvaluation`, `testUpdatePeerEvaluation`; `EvaluationIntegrationTest.testStudentJohnAddEvaluation` (covers the not-active-week, not-previous-week, not-same-team, and duplicate extensions) |
| UC-EVA-view-own-evaluation | The Student views her own peer evaluation report | ✅ Built | ✅ Verified | — | — | — | `MyEvaluations.vue` → `apis/evaluation` `generateWeeklyPeerEvaluationAveragesForStudentWithinOnePeriod`, `getWeeklyPeerEvaluationsForStudent` | `EvaluationController.generatePeerEvaluationSummariesForStudent` / `getWeeklyPeerEvaluationsForStudent → EvaluationService` / `PeerEvaluation` | `EvaluationServiceTest.testGenerateEvaluationSummariesForStudent`, `testGetWeeklyEvaluationsForStudent`; `EvaluationIntegrationTest.testStudentJohnGenerateOwnWeeklyPeerEvaluationSummaries`, `testStudentJohnGetDetailedWeeklyPeerEvaluationsOfSingleStudent` |
| UC-EVA-section-evaluation-report | The Instructor generates a peer evaluation report of the entire course section | ✅ Built | ✅ Verified | — | BO-PERF-faster-grading, BO-PERF-instructor-workload | — | `evaluations/admin/SectionsEvaluations.vue` → `apis/evaluation` `generateWeeklyPeerEvaluationAveragesForSection`, `getWeeklyPeerEvaluationsForStudent` | `EvaluationController.generateWeeklyPeerEvaluationReportForSection → EvaluationService.generateWeeklyPeerEvaluationReportForSection` / `PeerEvaluation` | `EvaluationServiceTest.testGenerateWeeklyPeerEvaluationReportForSection`; `EvaluationIntegrationTest.testAdminBingyangGenerateWeeklyPeerEvaluationForSection`, `testAdminTimGenerateWeeklyPeerEvaluationForSection` |
| UC-EVA-student-evaluation-report | The Instructor generates a peer evaluation report of a student | ✅ Built | ✅ Verified | — | BO-PERF-faster-grading, BO-PERF-instructor-workload | — | `evaluations/admin/StudentEvaluations.vue` → `apis/evaluation` `generateWeeklyPeerEvaluationAveragesForStudentWithinOnePeriod` | `EvaluationController.generatePeerEvaluationSummariesForStudent → EvaluationService.generateEvaluationSummariesForStudent` / `PeerEvaluation` (same method as UC-EVA-view-own-evaluation, RBAC-scoped) | `EvaluationServiceTest.testGenerateEvaluationSummariesForStudent`; `EvaluationIntegrationTest.testAdminBingyangGenerateWeeklyPeerEvaluationSummariesForSingleStudent`, `testStudentJohnGenerateAnotherStudentsWeeklyPeerEvaluationSummaries`, `testAdminTimGenerateAnotherStudentsWeeklyPeerEvaluationSummaries` |
| UC-TPL-provision-documents | The Course Admin creates team documents from built-in templates | ✅ Built | ✅ Verified | FR-TPL-enforce-structure, FR-TPL-section-keys | BO-RAM-requirement-quality, BO-RAM-learning-outcomes | — | `Teams.vue` → `apis/ram` `createDocument` | `DocumentController.createDocument` → `DocumentService.createRequirementDocument` + `DocumentTemplateRegistry` (template YAMLs) | `DocumentControllerTest.createVisionScopeDocument`, `…ByStudent` |
| UC-GLO-view-glossary | The Student views the Project Glossary | ✅ Built | ✅ Verified | — | — | — | `RamGlossary.vue` → `searchRequirementArtifacts` | `RequirementArtifactController.findRequirementArtifactsByCriteria` (+ `RequirementArtifactSpecs`) | `RequirementArtifactControllerTest.findRequirementArtifactsByCriteria1..3` |
| UC-GLO-find-terms | The Student finds glossary terms | ✅ Built | ✅ Verified | — | — | — | `RamGlossary.vue` (search/filter) → `searchRequirementArtifacts` | `RequirementArtifactController.findRequirementArtifactsByCriteria` (+ `RequirementArtifactSpecs`) | `RequirementArtifactControllerTest.findRequirementArtifactsByCriteria1..3` |
| UC-GLO-view-term | The Student views a glossary term | ✅ Built | 🔎 None | — | — | — | `RamGlossary.vue` → `getGlossaryTermById` | `GlossaryController.findGlossaryTermById` → `GlossaryService` | — |
| UC-GLO-create-term | The Student creates a glossary term | ✅ Built | 🔎 None | — | BO-RAM-consistency | — | `RamGlossary.vue` → `createGlossaryTerm` | `GlossaryController.createGlossaryTerm` → `GlossaryService` | — |
| UC-GLO-edit-term-definition | The Student changes a glossary term definition | ✅ Built | 🔎 None | — | BO-RAM-consistency | — | `RamGlossary.vue` → `updateGlossaryTermDefinition` | `GlossaryController.updateGlossaryTermDefinition` | — |
| UC-GLO-rename-term | The Student renames a glossary term | ✅ Built | 🔎 None | — | BO-RAM-consistency | — | `RamGlossary.vue` → `renameGlossaryTerm` | `GlossaryController.renameGlossaryTerm` | — |
| UC-GLO-delete-term | The Student deletes a glossary term | 🟡 In progress | 🔎 None | — | — | — | — (no delete control in `RamGlossary.vue`) | `RequirementArtifactController.deleteRequirementArtifact` (generic artifact delete; no glossary-specific endpoint) | — |
| UC-DOC-view-document | The Student views a section-based requirement document | ✅ Built | ✅ Verified | — | — | — | `RamDocuments.vue` → `searchDocuments`; `RamDocumentEditor.vue` → `findDocumentById` | `DocumentController.findDocumentById` / `findDocumentsByCriteria` → `DocumentService.findDocumentByIdWithFullGraph` | `DocumentControllerTest.findDocumentById*`, `findDocumentsByCriteria*` |
| UC-DOC-edit-document | The Student edits a section-based requirement document | ✅ Built | ✅ Verified | FR-SAVE-autosave-active, FR-VAL-background-recheck | — | — | `RamDocumentEditor.vue` → `findDocumentSectionById`, `updateDocumentSection`, `getDocumentSectionLock`, `lock`/`unlockDocumentSection` | `DocumentSectionController` → `DocumentSectionService` (+ `DocumentSectionLock`) | `DocumentSectionControllerTest.*` (update / lock / unlock / `*_LockedByAnotherUser` (lock-collision extension), `*_NotLocked` (edit-without-lock extension)) |
| UC-DOC-view-use-cases-document | The Student views the Use Cases document | ✅ Built | ✅ Verified | — | — | — | `RamUseCases.vue` (sidebar) → `searchRequirementArtifacts` | `RequirementArtifactController.findRequirementArtifactsByCriteria`; `UseCaseController` | `RequirementArtifactControllerTest.findRequirementArtifactsByCriteria1..3` |
| UC-DOC-view-use-case | The Student views a use case | ✅ Built | ✅ Verified | — | — | — | `RamUseCases.vue` → `getUseCaseById` | `UseCaseController.findUseCaseById` → `UseCaseService` | `UseCaseControllerTest.findUseCaseById` |
| UC-DOC-create-use-case | The Student creates a use case | ✅ Built | ✅ Verified | — | — | — | `RamUseCases.vue` → `createUseCase` | `UseCaseController.addUseCase` → `UseCaseService` | `UseCaseControllerTest.addUseCase`, `addUseCase_NotSameTeam` (cross-team-denial extension) |
| UC-DOC-edit-use-case | The Student edits a use case | ✅ Built | ✅ Verified | FR-SAVE-autosave-active, FR-VAL-background-recheck | — | — | `RamUseCases.vue` → `updateUseCase`, `getUseCaseLock`, `lock`/`unlockUseCase` | `UseCaseController.updateUseCase` / lock endpoints (+ `UseCaseLock`) | `UseCaseControllerTest.updateUseCaseWith*`, `lockUseCase*`, `unlockUseCase*` (lock/unlock + collision extensions) |
| UC-ART-find-artifacts | The Student finds requirement artifacts | 🟡 In progress | ✅ Verified | — | BO-RAM-navigation | — | — (no standalone artifact browser; artifacts surface embedded in `RamDocumentEditor.vue` / `RamGlossary.vue` / `RamUseCases.vue` via `searchRequirementArtifacts`) | `RequirementArtifactController.findRequirementArtifactsByCriteria` (+ `RequirementArtifactSpecs`) | `RequirementArtifactControllerTest.findRequirementArtifactsByCriteria1..3` (backend only) |
| UC-ART-view-artifact | The Student views a requirement artifact | 🟡 In progress | ✅ Verified | — | — | — | — | `RequirementArtifactController.findRequirementArtifactById` | `RequirementArtifactControllerTest.findRequirementArtifactById_Admin`, `_SameTeam`, `_NotSameTeam` (cross-team-denial extension; backend only) |
| UC-ART-create-artifact | The Student creates a requirement artifact | 🟡 In progress | ✅ Verified | — | — | — | — | `RequirementArtifactController.addRequirementArtifact` (+ `ArtifactKeySequence`) | `RequirementArtifactControllerTest.addRequirementArtifact_SameTeam`, `_NotSameTeam` (cross-team-denial extension; backend only) |
| UC-ART-promote-selection | The Student creates a requirement artifact from document content (promote selection) | 🚫 Tabled | — | — | — | — | — | — | — |
| UC-ART-edit-artifact | The Student edits a requirement artifact | 🟡 In progress | ✅ Verified | — | — | — | — | `RequirementArtifactController.updateRequirementArtifact` | `RequirementArtifactControllerTest.updateRequirementArtifact` (backend only) |
| UC-ART-delete-artifact | The Student deletes a requirement artifact | 🟡 In progress | 🔎 None | — | — | — | — | `RequirementArtifactController.deleteRequirementArtifact` | — |
| UC-LNK-view-links | The Student views requirement artifact links | 🟡 In progress | ✅ Verified | — | BO-RAM-traceability, BO-RAM-navigation | — | — | `ArtifactLinkController.findArtifactLinksByCriteria` (+ `ArtifactLinkSpecs`) | `ArtifactLinkControllerTest.findArtifactLinksByCriteria` (backend only) |
| UC-LNK-view-link | The Student views a requirement artifact link | 🟡 In progress | ✅ Verified | — | BO-RAM-traceability, BO-RAM-navigation | — | — | `ArtifactLinkController.getArtifactLinkById` | `ArtifactLinkControllerTest.findArtifactLinkById`, `_NotSameTeam` (cross-team-denial extension; backend only) |
| UC-LNK-create-link | The Student creates a requirement artifact link | 🟡 In progress | ✅ Verified | — | BO-RAM-traceability | — | — | `ArtifactLinkController.addArtifactLink` | `ArtifactLinkControllerTest.addArtifactLink`, `_NotSameTeam` (cross-team-denial extension; backend only) |
| UC-LNK-edit-link | The Student edits a requirement artifact link | 🟡 In progress | ✅ Verified | — | BO-RAM-traceability | — | — | `ArtifactLinkController.updateArtifactLink` | `ArtifactLinkControllerTest.updateArtifactLink` (backend only) |
| UC-LNK-delete-link | The Student deletes a requirement artifact link | 🟡 In progress | ✅ Verified | — | BO-RAM-traceability | — | — | `ArtifactLinkController.deleteArtifactLink` | `ArtifactLinkControllerTest.deleteArtifactLink` (backend only) |
| UC-LNK-trace-requirement | The Student traces a requirement across levels | 🟡 In progress | ✅ Verified | — | BO-RAM-traceability, BO-RAM-navigation | — | — | `ArtifactLinkController.getArtifactTraceabilityByRequirementArtifactId` → `ArtifactTraceability` DTO (upstream/downstream link views) | `ArtifactLinkControllerTest.findArtifactLinksForOneArtifact`, `_NotSameTeam` (cross-team-denial extension; backend only) |
| UC-VAL-run-validation | The Student runs validation (ReqLint) on the current document | ❌ Not started | — | — | BO-RAM-requirement-quality, BO-RAM-instructor-workload, BO-RAM-consistency | — | — | — | — |
| UC-COL-collaborative-edit | The Student collaboratively edits a requirement document | ❌ Not started | — | — | — | — | — | — | — |
| UC-COL-add-comment | The Student comments on a requirement document | 🟡 In progress | ✅ Verified | — | — | — | — | `CommentController` (`createCommentThreadForDocument` / `…ForDocumentSection` / `…ForRequirementArtifact`, `addCommentToCommentThread`, list/get) → `CommentService` | `CommentControllerTest.*` (list/create/get/add/update/delete comment & thread; backend only) |
| UC-COL-resolve-comment | The Student resolves a comment | 🟡 In progress | ✅ Verified | — | — | — | — | `CommentController.updateCommentThreadStatus` (+ `CommentThreadStatus`) | `CommentControllerTest.resolveACommentThread` (backend only) |
| UC-REV-submit-for-review | The Student submits requirements for review | 🟡 In progress | 🟡 Partial | — | — | — | — (no `apis/ram` status call) | `DocumentController.updateDocumentStatus` → `DocumentStatus.SUBMITTED` (review-workflow email not built) | `DocumentControllerTest.updateDocumentStatus`, `_NotSameTeam` (cross-team-denial extension; review-email path untested) |
| UC-REV-review-documents | The Instructor reviews a team's requirement documents | 🟡 In progress | 🟡 Partial | — | BO-RAM-instructor-workload | — | — | `DocumentController.updateDocumentStatus` → `DocumentStatus.RETURNED`, `findDocumentById` (review-workflow email not built) | `DocumentControllerTest.updateDocumentStatus`, `_NotSameTeam` (cross-team-denial extension; review-email path untested) |
| UC-EXP-export-document | The Student exports a requirement document | ❌ Not started | — | SI-export-formats, SI-export-fidelity | — | — | — | — | — |
| UC-EXP-export-bundle | The Student exports all the requirement documents as a bundle | ❌ Not started | — | SI-export-formats, SI-export-fidelity | — | — | — | — | — |
| UC-CFG-configure-teaching-context | The Instructor configures the teaching context for a course section | ❌ Not started | — | FR-AI-teaching-context | — | — | — | — | — |
| UC-CFG-toggle-assistants | The Instructor enables or disables AI assistants for a course section | ❌ Not started | — | FR-AI-enablement | — | — | — | — | — |
| UC-CFG-configure-assistant-instructions | The Instructor configures the assistant instructions for a course section | ❌ Not started | — | FR-AI-assistant-instructions | — | — | — | — | — |
| UC-CFG-configure-review-criteria | The Instructor configures the cross-document review criteria for a course section | ❌ Not started | — | — | — | — | — | — | — |
| UC-AI-import-source-material | The Student imports client pitch materials as project source | ❌ Not started | — | SI-import-allowlist, SI-import-extraction | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-elicit-requirements | The Student elicits requirements with the elicitation assistant | ❌ Not started | — | FR-AI-no-auto-edit, FR-AI-degradation, FR-AI-source-material-context | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-practice-interview | The Student practices a client interview with a role-playing client assistant | ❌ Not started | — | FR-AI-degradation | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-structure-notes | The Student turns meeting notes into structured requirements | ❌ Not started | — | FR-AI-rationale, FR-AI-no-auto-edit, FR-AI-degradation | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-critique | The Student requests a critique from the critique assistant | ❌ Not started | — | FR-AI-rationale, FR-AI-no-auto-edit, FR-AI-distinguish-suggestions, FR-AI-degradation | BO-RAM-requirement-quality, BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-tutor | The Student asks an assistant to explain a concept (Tutor Mode) | ❌ Not started | — | FR-AI-degradation | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-draft-skeleton | The Student generates a draft requirement skeleton with an assistant | ❌ Not started | — | FR-AI-no-auto-edit, FR-AI-distinguish-suggestions, FR-AI-enablement, FR-AI-degradation | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-review-proposal | The Student reviews and accepts or rejects an assistant proposal | ❌ Not started | — | — | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-consult-project-assistant | The Student consults the project assistant | ❌ Not started | — | FR-AI-no-auto-edit, FR-AI-enablement, FR-AI-degradation | BO-RAM-learning-outcomes | — | — | — | — |
| UC-AI-whole-project-review | The Student requests a whole-project review from the critique assistant | ❌ Not started | — | FR-AI-rationale, FR-AI-no-auto-edit, FR-AI-degradation | BO-RAM-requirement-quality, BO-RAM-instructor-workload, BO-RAM-learning-outcomes | — | — | — | — |

> The FR IDs column lists the **SRS's non-use-case cross-cutting subsystems** each use case depends on, *additive over the universal baseline* — RBAC (`FR-SEC-*`) and authorship metadata (`FR-HIS-authorship-metadata`) apply almost everywhere and are omitted from every row, so a row never being shown them does not mean it lacks them. `—` therefore marks a use case with **no additional cross-cutting FR beyond that baseline** — otherwise self-contained, its own steps its full spec (the common case for CRUD use cases — per the project's model, the use case *is* the functional requirement), **not** a use case with no security/authorship dimension. When planning a use case, treat its "The system …" steps + Associated Information as the acceptance criteria, and flag any step with no backing behavior.

> **UC-COL-collaborative-edit note:** section/use-case **locking and unlocking is built and verified** — but that scope belongs to UC-DOC-edit-document and UC-DOC-edit-use-case (both `✅ Built` / `✅ Verified`), which UC-COL-collaborative-edit references rather than owns. UC-COL-collaborative-edit's own scope is the real-time collaboration layer (teammate presence, join/leave notification, live broadcast of saved changes and lock state — the system steps of UC-COL-collaborative-edit itself), and **none of that is built**, so UC-COL-collaborative-edit is `❌ Not started`.

## Non-use-case FR traceability

Non-use-case functional requirements (`FR-<AREA>-*`) are **first-class requirement nodes** — siblings of the use case under each feature (a use case *is* a high-level FR; these are the cross-cutting ones no single use case owns: autosave, validation, AI, templates, glossary invariants, authorship metadata, notifications, security). The functional matrix above records them only as the *FR IDs* **dependency** column on UC rows; this matrix gives each its **own realization** row (design → code → tests → build/verify), so "is `FR-X` built and tested?" has an answer even when no use case carries it. Same Build and Verify legends as the functional matrix.

Two conventions keep this from duplicating the other matrices: a cross-cutting FR already verified through a quality scenario (the `FR-SEC-*` ↔ `QS-1`/`QS-2` pair) cites that scenario in **Tests** rather than restating it; and **deferred** non-use-case FRs carry no row until scheduled (mirroring deferred use cases) — `FR-HIS-checkpoint` / `-restore` / `-retention` and `FR-TPL-customize` are post-MVP and omitted here.

| FR | Supports BO | Design | Code | Tests | Build | Verify |
|----|-------------|--------|------|-------|-------|--------|
| FR-SAVE-autosave-active | — | — | `DocumentSectionController.updateDocumentSection` → `DocumentSectionService` (backend persistence) | `DocumentSectionControllerTest.*` (update); 10 s client-side cadence untested — cf. `QS-4` | 🟡 In progress | 🟡 Partial |
| FR-VAL-background-recheck | BO-RAM-requirement-quality, BO-RAM-instructor-workload, BO-RAM-consistency | — | — (ReqLint engine not built; cf. UC-VAL-run-validation ❌) | — | ❌ Not started | — |
| FR-AI-no-auto-edit | BO-RAM-learning-outcomes | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-distinguish-suggestions | — | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-teaching-context | — | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-enablement | — | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-rationale | BO-RAM-requirement-quality | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-degradation | — | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-source-material-context | — | — | — (AI not built) | — | ❌ Not started | — |
| FR-AI-assistant-instructions | — | — | — (AI not built) | — | ❌ Not started | — |
| FR-TPL-enforce-structure | BO-RAM-requirement-quality | — | `DocumentService.createRequirementDocument` + `DocumentTemplateRegistry` / `DocumentTemplate` (template YAMLs) | `DocumentControllerTest.createVisionScopeDocument` | ✅ Built | ✅ Verified |
| FR-TPL-section-keys | BO-RAM-requirement-quality | — | `DocumentTemplate` (section keys) + `DocumentService.createRequirementDocument` | `DocumentControllerTest.createVisionScopeDocument` | ✅ Built | ✅ Verified |
| FR-GLO-term-suggestion | BO-RAM-consistency | — | — (no term-suggestion logic) | — | ❌ Not started | — |
| FR-HIS-authorship-metadata | — | — | `JpaAuditingConfig` + `@CreatedBy`/`@LastModifiedBy` on RAM entities (`RequirementArtifact`, `RequirementDocument`, `DocumentSection`, `ArtifactLink`, `Comment`, …) | — (no targeted test; known gaps tracked in OI-19) | 🟡 In progress | 🔎 None |
| FR-NOT-suppress-routine | — | — | Holds by construction (no routine-edit notification code; `CI-no-routine-email`) | — (no negative test) | ✅ Built | 🔎 None |
| FR-NOT-weekly-reminder | BO-PERF-submission-rate, BO-PERF-faster-completion | — | `WeeklyReminderScheduler` (`system/`) → `EmailService.sendReminderEmail`, `SectionService` | — (no scheduler test) | ✅ Built | 🔎 None |
| FR-SEC-authentication | — | — | `SecurityConfiguration` + JWT (`AuthService`, `CustomBearerTokenAuthenticationEntryPoint`) | integration `isUnauthorized` cases → see NFR matrix `QS-2` | ✅ Built | ✅ Verified |
| FR-SEC-authorization | — | — | `*AuthorizationManager` family + `*SecurityService` (RBAC + team scoping) | `*_NotSameTeam` / `*NotInSameCourse` / `*NotSameSection` → see NFR matrix `QS-1` | ✅ Built | ✅ Verified |
| FR-SEC-deny-unauthorized | — | — | `CustomBearerTokenAccessDeniedHandler` + `CustomBasicAuthenticationEntryPoint` + `SecurityConfiguration` | integration `isForbidden` / `isUnauthorized` cases → see NFR matrix `QS-1`/`QS-2` | ✅ Built | ✅ Verified |

> **Why some `FR IDs` cells above repeat here.** An FR that appears in a UC row's *FR IDs* column (e.g. `FR-SAVE-autosave-active`, `FR-TPL-*`) is *depended on* by that use case; its row here records where **its own** code and tests live — a different relation (the model's *FR → design → code → tests* tail vs. the *UC depends on FR* edge). The previously untraced FRs this matrix newly homes: `FR-NOT-weekly-reminder` (built, `WeeklyReminderScheduler`), `FR-NOT-suppress-routine`, `FR-GLO-term-suggestion`, and `FR-SEC-deny-unauthorized`.

## Non-functional traceability matrix

The second axis: **quality attribute → quality scenario → verifying test**. Where the functional matrix above is keyed to the use case, this one is keyed to the SRS quality attributes (`USE-*`, `PER-*`, `SEC-*`, `AVL-*`, `ROB-*`, `SCA-*`, `INT-*`, `MNT-*`). Each is operationalized by a quality scenario (`QS-n`) in the [architecture-of-record](design/architectural-design.md#quality-requirements) — which owns the scenario *definitions* (stimulus / response / measure) — and verified by a test cited here. This file owns the **verification map** (the *Verifying test* and *State* columns); the arch-of-record's QS table points back here rather than duplicating them.

### How to use it

- **One row per committed quality scenario** in the first table; the `QS-n` cell cites the arch-of-record scenario by ID (definitions live there, not here).
- A quality attribute that no committed scenario yet operationalizes is **not** dropped silently — it goes in the *unpinned tail* table with its verification route, so an untraced NFR is visible rather than forgotten.
- When a new `QS-n` is added to the arch-of-record, add its row here; when `/implement` builds the feature a `❌ Pending feature` row waits on, fill the *Verifying test* and flip the *State*.

### Verification-state legend

| Symbol | Meaning |
|--------|---------|
| ✅ Verified | Automated test(s) exercise the scenario and pass. |
| 🟡 Half-verified | One part of the scenario is tested; another part is not. |
| ⚙️ Pipeline-verified | Verified by the CI/CD pipeline (deploy smoke check, migrations), not the test suite. |
| ⚠️ Process-verified | Relies on review/convention today; no automated guard. |
| 🧪 Empirically verified | Verified by a non-automated method (usability testing); not a code test. |
| ❌ Pending feature | Cannot be verified until the underlying feature is built. |

### Quality attribute → scenario → test

| Quality attribute(s) | QS | Verifying test (today) | State |
|---|---|---|---|
| `SEC-authorization` (honors `BR-team-scoped-access`, `BR-role-based-access`) | [QS-1](design/architectural-design.md#quality-requirements) | Cross-scope denial: `*_NotSameTeam` / `*NotInSameCourse` / `*NotSameSection` methods + `isForbidden` assertions across `RequirementArtifactControllerTest`, `DocumentControllerTest`, `ArtifactLinkControllerTest`, `ActivityIntegrationTest`, `CriterionIntegrationTest`, … | ✅ Verified |
| `SEC-authentication` | [QS-2](design/architectural-design.md#quality-requirements) | `isUnauthorized` cases on protected `/api/v1` endpoints in the integration suite (`CourseIntegrationTest`, `EvaluationIntegrationTest`, `InstructorIntegrationTest`, …) | ✅ Verified |
| `MNT-service-layer`, `INT-single-application` | [QS-3](design/architectural-design.md#quality-requirements) | — (no architecture-conformance test; convention is review-enforced) | ⚠️ Process-verified |
| `PER-autosave-cadence`, `ROB-edit-loss-bound`, `ROB-autosave-retry` | [QS-4](design/architectural-design.md#quality-requirements) | Collision half: `DocumentSectionControllerTest.*_LockedByAnotherUser` / `*_NotLocked`, `UseCaseControllerTest.lock*` / `unlock*` (✅). Autosave-cadence / edit-loss / retry half: client-side, **no test** (no RAM frontend test) | 🟡 Half-verified |
| `AVL-llm-degradation`, `PER-ai-response-time` | [QS-5](design/architectural-design.md#quality-requirements) | — (AI not built; `UC-AI-*` all ❌) | ❌ Pending feature |
| `AVL-uptime` | [QS-6](design/architectural-design.md#quality-requirements) | Flyway migration apply + staging-slot smoke check in `azure-webapps-deploy.yml` | ⚙️ Pipeline-verified |
| `PER-validation-speed`, `PER-graph-load`, `SCA-cohort-load` | [QS-7](design/architectural-design.md#quality-requirements) | — (ReqLint/`UC-VAL` not built; no load-test harness for graph-load) | ❌ Pending feature |

### Quality attributes with no committed scenario (unpinned tail)

These have no `QS-n` yet; the matrix records *why* and the verification route so none floats untraced.

| Quality attribute | Why no committed scenario | Verification route |
|---|---|---|
| `USE-wcag-aa`, `USE-keyboard-operable` | Accessibility "in scope but not yet architecturally addressed" — tracked as **TD-11** in the arch-of-record | Add a QS + `cypress-axe` / keyboard-only E2E when TD-11 is picked up |
| `SEC-ferpa` | Compliance umbrella, realized by composition of `SEC-authorization` (QS-1) + `SEC-https` | Verified-by-composition of QS-1 + TLS config; no dedicated test |
| `SEC-https` | TLS is deploy/infrastructure config | Verified by deployment configuration (outside the test suite) |
| `SEC-llm-proxy` | A structural confidentiality property (LLM credentials never reach the browser), not an operational scenario — and the AI proxy it constrains is unbuilt (`UC-AI-*` ❌) | Enforced by the server-side AI proxy; verify by code review / config that no LLM credential is exposed client-side (no automated test until the AI features land) |
| `USE-first-session-success` | 95%-success is an empirical target, not automatable | 🧪 Verified by usability testing |
| `USE-actionable-findings` | Depends on ReqLint / AI finding presentation | Folds into QS-4 / QS-5 once those features land |
| `PER-collab-latency`, `ROB-no-overwrite` | Post-MVP — depend on the deferred real-time collaboration (UC-COL-collaborative-edit) | Deferred; add scenarios with that feature |
| `SAF-not-applicable` | No safety functions exist (`SAF-not-applicable`) | N/A by definition |

> **Readout.** Security is the strongest NFR spine — QS-1/QS-2 are genuinely test-backed from the foundation work. QS-5 and QS-7 are honest `❌ Pending feature` rows (you cannot test ReqLint speed or LLM degradation before those features exist), and they make the perf/degradation tests part of "done" when `/implement` reaches `UC-VAL` and the AI use cases. The two gaps **not** waiting on a feature: QS-3 has no automated architecture guard (an ArchUnit test would move it to ✅), and QS-4's client-side half (autosave cadence, ≤10 s edit-loss) is untested even though the feature is built (a Cypress test could close it today).

## BO measurement matrix

Business objectives (`BO-*`, owned by [vision-and-scope.md](requirements/vision-and-scope.md#business-objectives)) sit at the **head** of the spine — the *why*. The functional and non-functional matrices above answer "is the objective **built**?"; this matrix answers the complementary, post-deployment question "was it **achieved**?" — the outcome → objective backward edge that ISO/IEC/IEEE 29148 calls for and that most projects leave implicit. Each objective's success metric (`SM-<slug>`, defined with its BO in `vision-and-scope.md`) is recorded here with its evaluation route and state.

Most routes are **empirical** — survey, system logs, rubric scoring, instructor judgment — **not automatable tests**: objective achievement is *validated after deployment*, not unit-tested, and naming the route keeps that honest rather than implying a code test exists. A platform-level emergent objective carries no metric (the `*(Platform-level emergent objective; …)*` carve-out). `/spec-build`'s **BO-measurement** check flags any non-carve-out `BO-*` with no `SM-<slug>`.

### Measurement-state legend

| Symbol | Meaning |
|--------|---------|
| 📊 Awaiting pilot | Instrument defined; no data yet (nothing deployed to a live cohort). |
| ⏳ Instrument pending feature | The measurement instrument itself depends on an unbuilt feature (manual stopgap until then). |
| ✅ Met / ❌ Not met | Measured against the target in a piloted term. |

| BO | Success metric | Target | Evaluation route | State |
|---|---|---|---|---|
| BO-PERF-faster-grading | SM-grading-time | −50% grading time | instructor time self-report, pre/post (1st pilot term) | 📊 Awaiting pilot |
| BO-PERF-instructor-workload | SM-perf-manual-steps | eliminate manual cycle (qualitative) | workflow step-count + instructor self-report | 📊 Awaiting pilot |
| BO-PERF-submission-rate | SM-submission-rate | +20% on-time rate | system submission logs vs LMS historical baseline | 📊 Awaiting pilot |
| BO-PERF-faster-completion | SM-completion-time | −25% completion time | student time survey | 📊 Awaiting pilot |
| BO-RAM-requirement-quality | SM-requirement-quality | improvement (qualitative) | instructor rubric scoring vs prior cohorts | 📊 Awaiting pilot |
| BO-RAM-instructor-workload | SM-ram-grading-workload | reduction (qualitative) | instructor self-report | 📊 Awaiting pilot |
| BO-RAM-consistency | SM-terminology-consistency | reduction (qualitative) | ReqLint terminology-violation counts; manual review until then | ⏳ Instrument pending feature (UC-VAL/ReqLint ❌) |
| BO-RAM-traceability | SM-traceability-completeness | reduction (qualitative) | requirements-graph orphan/link metrics (graph built; metrics report TBD) | 📊 Awaiting pilot |
| BO-RAM-learning-outcomes | SM-learning-outcomes | improvement (qualitative) | course learning-outcome assessment / instructor evaluation | 📊 Awaiting pilot |
| BO-RAM-navigation | SM-navigation-usage | students use trace navigation (qualitative) | feature-usage observation + instructor/student feedback | 📊 Awaiting pilot |
| BO-RAM-research | — (carve-out) | — | platform-level emergent objective; no success metric by design | — |

> **Readout.** Every objective now has an explicit way to tell whether it was *achieved*, not just shipped — closing the loop the spec→code matrices can't. The honest truth this surfaces: **none can be confirmed until a live pilot**, and most routes are empirical rather than automatable (the `📊 Awaiting pilot` rows), so "objective met" is a post-deployment finding, not a test result. One metric can't even be instrumented yet — `SM-terminology-consistency` rides ReqLint, which is unbuilt (`⏳`), with manual review as the stopgap. `BO-RAM-research` is the sole carve-out, consistent with its platform-level-emergent status in the forward-coverage check.

## Risk coverage matrix

Risks (`RI-*`, owned by [vision-and-scope.md](requirements/vision-and-scope.md#risks)) sit **off** the realization spine: they *motivate* objectives rather than being "realized by" code (see [methodology.md, *The traceability model*](methodology.md#the-traceability-model)). But a named risk that nothing responds to is as much a gap as an unimplemented objective, so each risk must trace **forward** to the mitigating requirement(s) or decision that addresses it — or be **consciously accepted**. This matrix records that forward edge and, wherever possible, the **verification route** the mitigation already rides (an existing quality scenario, quality attribute, or deployment guard), reusing the non-functional spine above rather than inventing a parallel one.

A risk with no software mitigation is not dropped silently: it carries an inline `*(Accepted; <rationale>)*` marker in `vision-and-scope.md` (parallel to the business-rule `*(Advisory; …)*` and business-objective `*(Platform-level emergent objective; …)*` carve-outs), and appears here marked **Accepted**. `/spec-build`'s **risk-coverage** check flags any `RI-*` that is neither given a mitigation row here nor marked accepted there.

| Risk | Mitigation / disposition | Verification route |
|---|---|---|
| RI-data-breach | `SEC-authentication`, `SEC-authorization`, `SEC-https`, `SEC-ferpa`, `FR-SEC-*` | QS-1 + QS-2 (✅ Verified); TLS by deploy config; FERPA by composition |
| RI-accessibility | `USE-wcag-aa`, `USE-keyboard-operable` | ❌ Pending — TD-11 in the arch-of-record (`cypress-axe` / keyboard-only E2E); see the NFR unpinned tail |
| RI-scalability | `SCA-cohort-load`, the `PER-*` concurrency envelope | QS-7 (❌ Pending feature — no load-test harness yet) |
| RI-llm-reliability | `BR-assistant-socratic`, `FR-AI-rationale`, `FR-AI-no-auto-edit`; instructor-tunable teaching context (`UC-CFG-configure-teaching-context`) + assistant instructions (`FR-AI-assistant-instructions`) | ❌ Pending feature (AI not built; `UC-AI-*` / `UC-CFG-*` all ❌) |
| RI-over-reliance-on-ai | `BR-assistant-socratic`, `BR-explicit-acceptance`, `FR-AI-no-auto-edit`; drafting assistant off by default (`BR-assistant-enablement`, `FR-AI-enablement`) | ❌ Pending feature (AI not built) |
| RI-adoption-resistance | `USE-first-session-success` and the deliberately non-rigid Socratic design; partly accepted (pilot rollout, `AS-faculty-buy-in`) | 🧪 Usability testing (`USE-first-session-success` target: 95%); not automatable |
| RI-cross-browser | **Accepted** — supported set bounded by `OE-supported-browsers`; conformance by manual cross-browser testing | ⚠️ Manual cross-browser testing; no automated guard today |
| RI-cloud-cost | **Accepted** — Computer Science Department owns the cloud budget (business decision) | n/a (no software mitigation) |
| RI-narrow-applicability | **Accepted** — reuse/adoption risk; post-MVP `FR-TPL-customize` partially broadens applicability | n/a in MVP (revisit when template customization lands) |

> **Readout.** Every risk now traces somewhere. The security and scalability risks ride the existing NFR spine (QS-1/QS-2 today; QS-7 pending the load harness), and the AI risks are answered by the Socratic-assistant business rules and `FR-AI-*` — but their verification is honestly `❌ Pending feature` until the AI use cases are built, which means "mitigated in spec, not yet in code." The three `Accepted` rows are business risks with no software lever; the marker keeps them visibly accepted rather than silently untraced. The one mitigation with a verification gap *not* waiting on a feature is RI-cross-browser (no automated cross-browser/E2E guard — a Cypress/BrowserStack matrix would close it).

## Business-rule enforcement index

A business rule (`BR-*`, owned by [business-rules.md](requirements/business-rules.md)) is **cited, never restated**, and so has no realization row — it is traced *transitively* through whatever cites it (see [methodology.md, *The traceability model*](methodology.md#the-traceability-model)). The forward question — "is every rule enforced by something?" — is answered by `/spec-build`'s business-rule coverage check. This index answers the **backward** question — "given one rule, what is the *complete* set of requirements that enforce it?" — the view you need for **change-impact analysis** (reword `BR-lock-expiry` and you want every use case, FR, and quality scenario that depends on it, at a glance).

> ⚙️ **Generated, do not hand-edit.** This table is a *projection* of the forward citations — the use cases' **Business Rules** fields, the SRS's rule citations, and the NFR matrix's `QS-n` rows — not a second authored copy of them (that would violate "cited, never restated" and drift). `/spec-build` **regenerates and verifies** it against the live citations; a row that disagrees with them is a flagged defect, and edits belong in the citing artifacts, not here. The two universal access rules (`BR-role-based-access`, `BR-team-scoped-access`) are cited by essentially every authoring/CRUD use case and are summarized by count rather than enumerated (the same noise-reduction convention as the functional matrix's omitted `FR-SEC-*` column). This index is the **spec-level** backward view (which requirements *cite* a rule); its **code-level** companion — which `file:method` *enforces* each rule — lives in `/sync-check`'s `BR → enforcement-point` map. Together they trace a rule rule → requirements → code.
>
> **Last regenerated:** 2026-06-26 (against the citations as of this commit).

| Business rule | Cited by use cases | SRS | QS |
|---|---|---|---|
| `BR-account-self-setup` | `UC-ACC-setup-instructor-account`, `UC-ACC-setup-student-account` | — | — |
| `BR-active-weeks` | `UC-EVA-submit-evaluation`, `UC-SEC-setup-active-weeks` | — | — |
| `BR-artifact-key-unique` | `UC-ART-create-artifact` | (prose) | — |
| `BR-assistant-enablement` | `UC-AI-consult-project-assistant`, `UC-AI-draft-skeleton`, `UC-AI-elicit-requirements`, `UC-AI-practice-interview`, `UC-CFG-toggle-assistants` | — | — |
| `BR-assistant-socratic` | `UC-AI-consult-project-assistant`, `UC-AI-critique`, `UC-AI-draft-skeleton`, `UC-AI-elicit-requirements`, `UC-AI-practice-interview`, `UC-AI-review-proposal`, `UC-AI-structure-notes`, `UC-AI-tutor`, `UC-AI-whole-project-review` | — | — |
| `BR-authored-prevails` | `UC-AI-elicit-requirements` | — | — |
| `BR-collab-no-overwrite` | `UC-COL-collaborative-edit` | `ROB-no-overwrite` | — |
| `BR-comment-access` | `UC-COL-add-comment`, `UC-COL-resolve-comment` | — | — |
| `BR-deletion-integrity` | `UC-ART-delete-artifact`, `UC-GLO-delete-term` | (prose), `FR-HIS-authorship-metadata` | — |
| `BR-document-creation` | `UC-TPL-provision-documents` | — | — |
| `BR-edit-lock-required` | `UC-COL-collaborative-edit`, `UC-DOC-edit-document`, `UC-DOC-edit-use-case` | (prose) | — |
| `BR-evaluation-editable-until-close` | `UC-EVA-submit-evaluation` | — | — |
| `BR-evaluation-private-comment` | `UC-EVA-section-evaluation-report`, `UC-EVA-student-evaluation-report`, `UC-EVA-submit-evaluation`, `UC-EVA-view-own-evaluation` | — | — |
| `BR-evaluation-submission-window` | `UC-EVA-submit-evaluation` | — | — |
| `BR-evaluation-visibility` | `UC-EVA-view-own-evaluation` | — | — |
| `BR-explicit-acceptance` | `UC-AI-critique`, `UC-AI-draft-skeleton`, `UC-AI-review-proposal`, `UC-AI-structure-notes`, `UC-AI-whole-project-review` | — | — |
| `BR-glossary-term-unique` | `UC-GLO-create-term`, `UC-GLO-rename-term` | — | — |
| `BR-instructor-lifecycle` | `UC-INS-deactivate-instructor`, `UC-INS-reactivate-instructor` | — | — |
| `BR-invitations-admin-only` | `UC-INS-invite-instructors`, `UC-STU-invite-students` | — | — |
| `BR-link-constraints` | `UC-LNK-create-link`, `UC-LNK-edit-link` | — | — |
| `BR-lock-expiry` | `UC-DOC-edit-document`, `UC-DOC-edit-use-case` | (prose) | — |
| `BR-review-authority` | `UC-REV-review-documents` | (prose) | — |
| `BR-review-lock` | `UC-REV-submit-for-review` | (prose) | — |
| `BR-role-based-access` | _universal_ — 37 UCs (every authoring/CRUD use case) | `FR-SEC-authorization`, `SEC-authorization` | `QS-1` |
| `BR-rubric-admin-only` | `UC-RUB-add-criterion`, `UC-RUB-create-criterion`, `UC-RUB-create-rubric`, `UC-RUB-delete-criterion`, `UC-RUB-delete-rubric`, `UC-RUB-edit-criterion`, `UC-RUB-edit-rubric`, `UC-RUB-remove-criterion` | — | — |
| `BR-section-admin-only` | `UC-RUB-assign-rubric`, `UC-SEC-create-section`, `UC-SEC-edit-section`, `UC-SEC-setup-active-weeks` | — | — |
| `BR-section-config-access` | `UC-CFG-configure-assistant-instructions`, `UC-CFG-configure-review-criteria`, `UC-CFG-configure-teaching-context`, `UC-CFG-toggle-assistants` | — | — |
| `BR-source-material-import` | `UC-AI-import-source-material` | — | — |
| `BR-student-lifecycle` | `UC-STU-deactivate-student`, `UC-STU-delete-student`, `UC-STU-reactivate-student` | — | — |
| `BR-team-admin-only` | `UC-INS-assign-instructors`, `UC-INS-remove-instructor`, `UC-TEA-assign-students`, `UC-TEA-create-team`, `UC-TEA-delete-team`, `UC-TEA-edit-team`, `UC-TEA-remove-student` | — | — |
| `BR-team-scoped-access` | _universal_ — 26 UCs (every authoring/CRUD use case) | `FR-SEC-authorization`, `SEC-authorization`, `SI-export-formats` | `QS-1` |
| `BR-team-single-instructor` | `UC-INS-assign-instructors`, `UC-INS-remove-instructor` | — | — |
| `BR-use-case-name-unique` | `UC-DOC-create-use-case` | — | — |

> **Readout.** All 33 rules have at least one enforcer — the backward view confirms the forward coverage check from the other direction. Two rules are load-bearing (`BR-role-based-access` 37 UCs, `BR-team-scoped-access` 26 UCs) — touching either is a project-wide change, which is exactly what the index makes visible. The `(prose)` SRS entries are rules the SRS cites in narrative (e.g. the locking rules in the autosave/persistence discussion) rather than from a specific `FR-*`.
