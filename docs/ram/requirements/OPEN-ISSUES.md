# Open Issues & Backlog — RAM Requirements Docs

Working backlog of everything still needed to get the requirements docs implementation-ready. Each item has a stable ID (`OI-n`) so we can pick one and say "let's do OI-3." Tackle them roughly in priority order (P0 → P3), but they're independent — grab whichever fits the session.

**Last updated:** 2026-06-07

## How to use this file

- Check the box (`- [x]`) when an item is done; add a one-line note on what was decided/changed.
- **Owner tags:** ✍️ = I (Claude) can draft it, you confirm · 🧑 = needs your decision · 🔢 = needs a value/content only you have.
- New gaps we discover get appended with the next free `OI-n` (append-only IDs, like the BR/UC scheme).

---

## P0 — Implementation blockers (do first)

- [x] **OI-1 — Inline the data model (SRS §7.1).** ✅ *2026-06-04 — Inlined the corrected MVP `classDiagram` (Team / RequirementDocument / DocumentSection / DocumentSectionLock / RequirementArtifact / ArtifactLink / CommentThread / Comment + use-case decomposition) into §7.1 with prose + conceptual-model mapping; repointed §1.4; dropped the external mermaidchart link.*
- [x] **OI-2 — Resolve `traceability.md` FR wildcards.** ✅ *2026-06-04 — Removed every `FR-<area>*` wildcard. Per the project model (a use case is itself a high-level FR), the FR-IDs column now lists the SRS §5.2 cross-cutting subsystems each UC depends on (locking, autosave, validation, collaboration, export, AI, …) and `—` where the use case is self-contained; the universal FR-HIS-4 / FR-SEC-* are noted as applying everywhere. Updated the matrix's "How to use it" + footnotes accordingly.*

## P1 — Spec decisions that shape what gets built

- [x] **OI-3 — Commenting has no FR.** ✅ *2026-06-04 — Resolved by the methodology, not a new FR: per the use-cases-are-FRs model, UC-COL-2 (comment) and UC-COL-3 (resolve) **are** the functional requirements for commenting — their steps + Associated Information are the spec. The real fix was the policy: added **BR-20** (who may comment/resolve, and that commenting is allowed regardless of the review lock — for team Students and the Course Section's Instructor), resolved the UC-COL-2/UC-REV-2 contradiction, relaxed UC-COL-2 PRE-3, and added "status OPEN" to UC-COL-2's postcondition. (A short-lived `FR-CMT-1/2` was drafted then rolled back as duplicative.)*
- [x] **OI-4 — Version history has no UC.** ✅ *2026-06-05 — Decision: drop document version history from the MVP, deferred to a future release (not abandoned). `FR-HIS-1/2/3` (checkpoint, restore, retention) are retained in SRS §5.2.8 with their IDs but relabeled "Deferred — future release"; no use case is added (the gap is closed by removing the committed feature, not by adding a UC). `FR-HIS-4` (authorship metadata — creator/editor + timestamps) is **not** version history and stays in MVP scope. Vision describes version history as a post-MVP feature (consistent with OI-5/OI-6): §4.2.6 carries a post-MVP note, the §3.1 student stakeholder row lists it, and the §4.2 MVP exclusion list records it as deferred. §5.2.8 retitled "Authorship Metadata and Document Versioning Requirements" (TOC resynced).*
- [x] **OI-5 — Change-impact analysis + traceability matrix view.** ✅ *2026-06-05 — Decision: defer to a future release (post-MVP). The cluster covers the interactive traceability-matrix view, graph visualization (BO-6), highlighting of missing/incomplete linkages and orphan surfacing, and change-impact analysis — none had a UC or FR. The underlying typed links and trace navigation stay in MVP (UC-LNK-1..6; safe rename via UC-GLO-6 + FR-VAL-4). Vision updated: post-MVP notes added to §4.2.1, §4.2.4, and BO-6, and the capabilities added to the §4.2 MVP exclusion list. No UC/FR added; revisit when scoping a future release.*
- [x] **OI-6 — Broad smart-editing / auto-suggest.** ✅ *2026-06-05 — Decision: defer to a future release (post-MVP). Only glossary-term suggestion (`FR-GLO-3`) ships in the MVP; broader auto-suggestions (actors, requirement patterns, cross-document links) and auto-complete for requirement types/scenarios are deferred. Vision updated: post-MVP note added to §4.2.3, to-be process-flow step 6 reworded, and the capability added to the §4.2 MVP exclusion list. No new FR added.*
- [x] **OI-14 — Add the Requirement Link / graph layer to the §7.1 data model.** ✅ *2026-06-04 — Obviated by the corrected data model: `ArtifactLink` + `ArtifactLinkType` (and the outgoing/incoming relationships) are present in §7.1. The "graph-first" framing is consistent with the schema; no reconciliation needed.*
- [x] **OI-16 — Spec the whole-project cross-document review capability.** ✅ *2026-06-06 — Specced as a dedicated MVP use case. Added **UC-AI-10** (Request a whole-project review from the critique assistant): reviews **all** the team's requirement documents together (not hardcoded to five) for cross-document gaps/inconsistencies/conflicts/broken traceability; read-only; act on findings via UC-DOC-2/6 or UC-AI-8; reachable via a "Review whole project" action and routed from the project assistant (UC-AI-9, wired). Realized by new **FR-AI-21** (review behavior), **FR-AI-22** (include criteria in context), **FR-AI-23** (unavailable while criteria undefined). Per the user's added requirement, the review is governed by instructor/admin-configured **Cross-Document Review Criteria** — new glossary term + new **UC-CFG-4** to author them per course section; **BR-4** extended to make them instructor/admin-only; defining them is a precondition of UC-AI-10. Traceability rows added for UC-CFG-4 and UC-AI-10; TOCs regenerated; the scope note in `spec-driven-setup/cross-document-review-criteria.md` updated to point at the new UC/FRs (its Part 1/2 are now the default criteria + system prompt).*
- [x] **OI-17 — Define the artifact link-type compatibility matrix.** ✅ *2026-06-06 — Defined the matrix as an elaboration of BR-8 in `business-rules.md` (the single source of truth, chosen over UC-LNK-3 to co-locate with the rule it enforces). Rather than a 19×19×6 grid, it classifies requirement artifact types into four abstraction tiers (Tier 1 Business/Vision → Tier 4 Detailed/System) plus cross-cutting types (Stakeholder, Risk, Assumption, Dependency, Business Rule, Glossary Term, Background), then gives per-link-type source→target rules: DERIVES_FROM/REALIZES require source tier ≥ target tier (no high→low), REFERENCES is the any→any catch-all (only link allowed to touch Glossary Term/Background), IMPACTS from cross-cutting/constraining types → any requirement, MITIGATES → a Risk target, MOTIVATES from Stakeholder/Business Problem/Opportunity → any requirement. BR-8 amended to point to it; UC-LNK-3 (step 6) and UC-LNK-4 now reference the matrix instead of restating link types. Flagged dependency on OI-15(a): the type set (esp. Stakeholder/Dependency, which the §7.1 enum omits) must be revisited when OI-15 lands. (Surfaced 2026-06-04.)*

## P2 — Fill in committed values & finish partial use cases

- [ ] **OI-19 — Align RAM entity audit metadata with FR-HIS-4.** ✍️ *Surfaced 2026-06-07 during source-code/entity review. The SRS requires creator/editor identity plus created/last-modified timestamps for every authored item. Current entity gaps to fix later: `RequirementDocument` has timestamps but no `createdBy`/`updatedBy`; `DocumentSection` has creator/editor fields but lacks `@CreatedBy`/`@LastModifiedBy`; `Comment` has an author but no last-editor field; `UseCaseMainStep`, `UseCaseExtension`, and `UseCaseExtensionStep` have no audit metadata. Decide whether `UseCase` itself relies on its paired `RequirementArtifact` audit record or gets explicit audit fields too.*
- [x] **OI-7 — SRS §9 quality-attribute thresholds.** ✅ *2026-06-06 — Committed by the user: USE-4 first-session success **95%**; PER-3 ReqLint **3s** for 95% of runs; PER-4 AI response **15s**; SCA-1 **~70 students (~75 total users)**, peak concurrency within PER-1's 100-concurrent envelope (kept as headroom above the real ~70 peak). All four `TBD` notes removed; §9 now carries no placeholders.*
- [x] **OI-8 — Incomplete use-case fields.** ✅ *2026-06-06 — Filled the three remaining TBD/TODO fields. UC-LNK-2 description: rationale is now "understand the type and direction of the relationship between the two connected artifacts and navigate from one to the other." UC-ART-1: completed the search-criteria table (Keyword / Artifact key / Artifact content / Type — data types, optionality, fuzzy/case-insensitive matching, team-scoped per BR-1/BR-2, glossary refs), set the display strategy (Artifact Key, Type, content excerpt, last-modified by/date) and sort (default ASC by Artifact Key, re-sortable by Type or last-modified). UC-DOC-2: replaced the per-document section-rules TBDs with a pointer — section set/order/rules are defined by each document's built-in Template (UC-TPL-1); full per-section enumeration stays with the template (UC-TPL-1), not duplicated here.* (The notification fields were resolved 2026-06-05: routine changes are not notified, FR-NOT-2; only the review workflow notifies, by email, FR-NOT-1.)
- [x] **OI-15 — Reconcile the implementation enums (§7.1) with the glossary and the doc set.** ✅ *2026-06-06 — Reconciled against the actual codebase enums (`team.projectpulse.ram`), with the user treating them as input, not frozen (open to code changes for consistency + completeness).*
  - **(a) `RequirementArtifactType`** — settled the canonical 22-type taxonomy and aligned the glossary "Requirement Artifact" list one-to-one with the SRS §7.1 enum. Decisions: **collapse `BUSINESS_RISK`/`RISK` → single umbrella `RISK`** (covers business/adoption, technical/feasibility, and security/safety risks via an optional category — chosen so `MITIGATES`/`IMPACTS` can target any risk or threat, not just business risk; Vision §2.5 renamed "Business Risks" → "Risks"); **add `DEPENDENCY`** as a tracked artifact (Vision §2.6); **keep** `STAKEHOLDER`, `PRECONDITION`, `POSTCONDITION`, `SUCCESS_METRIC`; **remove `background`** from the artifact list (it's prose, not an artifact); `OTHER` documented as an implementation fallback. **Codebase deltas for the user:** remove `BUSINESS_RISK` (keep `RISK`), add `DEPENDENCY` (migrate existing `BUSINESS_RISK` rows → `RISK`).
  - **(b) `DocumentType` / user stories** — keep `USER_STORY` (artifact) and `USER_STORIES` (document) as a **recognized-but-deferred, post-MVP** concept (no MVP document/template/UC), per the user (other instructors may want user stories; not implemented this MVP). Marked deferred in glossary + SRS §7.1.
  - **(c) Placement map** — added the canonical **artifact type → document/section** map to SRS §7.1 (MVP places by the section the Student is in, UC-ART-3; the map is the basis for the future add-by-type path).
  - **OI-17 consumer updated:** the BR-8 link-type matrix now targets a single umbrella `Risk` (MITIGATES/IMPACTS), keeps `Dependency` as a real type, drops `Background`, classifies preconditions/postconditions as Tier 3, and its OI-15 caveat is removed.
  - **Assumption/dependency key spaces unified (2026-06-06):** the two clashing per-document `AS-*` runs violated the graph-first rule that Artifact Keys are team-unique (BR-5). Fixed by making `AS-*`/`DE-*` single team-wide sequences: Vision §2.6 keeps business assumptions `AS-1`…`AS-5`; SRS §4.5 architecture assumptions renumbered `AS-1/2/3` → `AS-6/7/8`; dependencies stay `DE-1/2/3` (SRS only). The "local/independent" disambiguation note was replaced with the team-wide framing. Assumptions are still authored in both docs (business vs architecture), per the kept split.
- [x] **OI-9 — Multi-hop graph expansion (design).** ✅ *2026-06-06 — Decided: **lazy, cycle-safe, and unbounded in depth** (it's a graph — multi-hop traversal is not artificially capped). UC-LNK-1 (free exploration) defaults to one hop and lets the Student expand any displayed artifact one hop at a time on demand and keep going to any depth; the System lazy-loads each hop (responsive as the view grows) and never re-expands an already-shown artifact, so cycles terminate and the displayed set stays finite. Added extension 5a + Associated Information to UC-LNK-1 and cleared its open issue. UC-LNK-6 is the complementary **ordered, goal-driven** path along abstraction levels (Objective→Feature→…→FR) — also multi-level, lazy, cycle-safe; the LNK-1↔LNK-6 distinction is free exploration vs ordered path, not shallow vs deep. UC-LNK-2 (single-link details) is terminal: removed its mis-placed multi-hop open issue and pointed it at UC-LNK-1/UC-LNK-6. (Initial draft had a depth-3 cap on LNK-1; removed it — the lazy + cycle-safe properties already keep the view finite without limiting how far a student can trace.)*

## P3 — SRS completeness & housekeeping (before final delivery, not before code)

- [x] **OI-10 — SRS intro (§1.1–1.3).** ✅ *2026-06-06 — Filled, reference-first (no duplication, per the user). §1.1 Purpose of the Project: a meaty summary of what RAM is and who uses it, pointing to Vision and Scope (§3) for the full motivation rather than restating it. §1.2 Purpose of this Document: what the SRS covers (§4–§12) and the "link, don't restate" principle. §1.3 Document Conventions: the ID schemes (`FR-<AREA>-<n>`, `UC-<AREA>-<n>`, `BR-<n>`, per-type artifact keys, SRS-local `CO/OE/UI/SI/CI` + §9 quality codes), EARS/"shall" + use-case-is-an-FR note, Markdown-canonical, and the single-source-of-truth/reference convention. Also cleaned the leftover `[URL]` gdoc stub in §1.4.*
- [x] **OI-11 — SRS external interfaces (§8).** ✅ *2026-06-06 — Filled §8 from the §4 architecture, constraints (CO-1…CO-7), and the export/import/AI/notification FRs. §8.1 User Interfaces (UI-1…UI-3: Project Pulse SPA conventions, WCAG 2.1 AA, the two-column locking editor). §8.2 Software Interfaces: SI-1 external LLM Service via the server-side AI proxy (CO-6/SEC-4, context payload, JSON/structured output, credentials server-side, graceful degradation), SI-2 Project Pulse host platform (SSO identity/roles, course/team data reuse, relational-DB persistence, Gmail relay), SI-3 document export (PDF/DOCX/Markdown + bundle), SI-4 Project Source Material import (PDF/PPTX allowlist, text extraction). §8.3 API link text cleaned. §8.5 Communications: CI-1 review-workflow email via Gmail SMTP, CI-2 no email for routine changes (real-time instead), CI-3 HTTPS to the LLM service, CI-4 HTTPS browser↔server. All cited FRs/CO/UC IDs verified to resolve.*
- [x] **OI-12 — SRS §10 Deployment, §11 i18n, §12 Other.** ✅ *2026-06-06 — Filled, reference-first. §10 Deployment: RAM deploys as part of Project Pulse (CO-1/INT-1); the target environment specifics (Azure App Service, GitHub Actions CI/CD, OpenAI, load testing) are referenced to Vision §4.3, not restated; notes versioned DB migrations for the graph schema. §11 i18n: N/A for release 1.0 (TCU, U.S. English only; accessibility is a usability req, USE-1/§9.1, not i18n). §12 Other: no new requirements — references the cross-cutting concerns to where they already live (FERPA CO-5/SEC-3; SSO+RBAC FR-SEC-1..3; authorship FR-HIS-4; audit/soft-delete BR-12; install/startup via §10). All cited IDs verified. **Confirm:** the §11 "U.S. English only, no localization" assumption for release 1.0.*
- [x] **OI-13 — Cosmetic housekeeping.** ✅ *2026-06-06 — Replaced all 23 Frequency-of-Use `\*\*\* user` / `\*\*\* per milestone` template placeholders across the use cases with honest qualitative values ("Varies by team and project phase; most frequent during active authoring and near deadlines"; the two review use cases get "Around each project review milestone") — no fabricated per-week numbers. No `\*\*\*` residue remains in `use-cases.md`. The two remaining sub-items are **event-triggered, not open work**: Revision History tables are filled on version finalize (per CLAUDE.md, only when explicitly asked), and the `traceability.md` relocation note + `root-CLAUDE.md` path reference are reconciled if/when the docs are relocated into the project-pulse repo.*

- [x] **OI-18 — Apply the "no special formatting for glossary terms" convention doc-wide.** ✅ *2026-06-07 — Adopted and documented the two-register convention: glossary terms are plain lowercase prose by default, with Title Case reserved for headings, use-case titles, UI/table labels, document titles, code identifiers/enums, and artifact-type controlled labels in taxonomy/placement contexts. Added the convention to `CLAUDE.md` and `README.md`; applied it across the five core requirements docs.*

---

## Change log

*(Chronological session notes. Completed `OI-n` items stay checked in place in their priority section above — keep the stable IDs there; this log records dated outcomes and work that never had an `OI-n`.)*

- **2026-06-04** — Authored `business-rules.md` (BR-1…BR-19) as the fifth core doc; wired CLAUDE.md / README / build command / root-CLAUDE.md.
- **2026-06-04** — Reconciled MVP assistant list (all but drafting); deferred dashboards/grading to post-MVP (review+comments only in MVP).
- **2026-06-04** — Filled SRS §9 Quality Attributes from committed facts (USE/PER/SEC/SAF/AVL/ROB/SCA/INT); added missing FR-COL-1.
- **2026-06-04** — Reverted glossary Artifact Key examples to the MVP's per-type running sequence (`FR-1`, `UC-1`).
- **2026-06-04** — **OI-1**: inlined the corrected MVP data model (Team/Document/Section/Lock/Artifact/Link/CommentThread/Comment + use-case decomposition) into SRS §7.1 with prose and conceptual-model mapping; repointed §1.4; removed the external mermaidchart link.
- **2026-06-04** — **OI-14** resolved: the corrected schema already includes the `ArtifactLink`/`ArtifactLinkType` graph layer. Opened **OI-15** (reconcile `RequirementArtifactType` enum vs glossary taxonomy); noted on OI-3 that comment entities now exist.
- **2026-06-04** — Wrote `spec-driven-setup/cross-document-review-criteria.md` (review methodology + critique-assistant system prompt for whole-project review). Opened **OI-16** to decide whether project-wide review becomes its own UC/FR.
- **2026-06-04** — **Clarified the use-cases-are-FRs model.** A use case *is* a high-level functional requirement; its "System shall" steps + Associated Information are its detailed spec. SRS §5.2 is reserved for non-use-case, system-level behaviors only. Initially drafted ~33 per-use-case CRUD FRs, then **rolled them back as duplicative** of the use cases (verifying each was covered first, folding the few missing details into the use cases: UC-ART-2 view fields, UC-LNK-5 deletion strategy, UC-COL-2 "status OPEN"). **Kept:** `FR-HIS-4` (cross-cutting authorship metadata), **BR-20** (comment access/lock policy) + the UC-COL-2/UC-REV-2 contradiction fix, the Markdown alignment in `FR-EXP-1`. Captured the principle in CLAUDE.md / README / build command / root-CLAUDE.md / feature.md. Decisions: artifacts edit without a lock; no BR for type→placement. Opened **OI-17** (link-type matrix); resolved **OI-3**.
- **2026-06-04** — **FR ID migration:** moved all SRS §5.2 FR IDs to the descriptive `FR-<AREA>-<n>` format (parallel to `UC-<AREA>-<n>`) — 278 replacements across 12 files. Resolved **OI-2** (traceability FR-IDs column now lists each UC's §5.2 cross-cutting dependencies, `—` where self-contained).
- **2026-06-05** — **Implementation-readiness audit** of all 32 active use cases (use cases + §5.2 vs FR coverage). Most are build-ready; remaining gaps logged. **Resolved notifications** (the biggest, cross-cutting gap): decided routine authoring changes are *not* notified (they propagate via real-time collaboration, new `FR-NOT-2`), and only the review workflow notifies — by email via Gmail SMTP (new `FR-NOT-1`, §5.2.13). Filled every notification field across GLO/DOC/ART/LNK/COL use cases accordingly; UC-REV-1/2 now specify email; added `FR-NOT` to the convention-doc area lists and traceability. Remaining audit gaps: UC-VAL-1 (ReqLint rule catalog), UC-TPL-1 (built-in template contents), plus the OI-8 TBDs, OI-9, OI-17.
- **2026-06-05** — **UC-ART-3 made implementation-ready.** Placement model decided: MVP places an artifact in the **Document Section the Student is in** (the section sets its placement and type — no type field, no destination picker); the future requirements-graph "add" path picks the section or auto-places by type. Duplication = **advisory fuzzy warning** (same type + similar content), non-blocking; rewrote the 5a exception accordingly. Aligned UC-ART-5 (type fixed by section). Deferred the type→section placement *map* to **OI-15(c)** (needs the canonical type set).
