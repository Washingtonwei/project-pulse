# Open Issues & Backlog — Requirements Docs & RAM Product

Working backlog of everything still needed to get the requirements docs implementation-ready. Each item has a stable ID (`OI-n`) so we can pick one and say "let's do OI-3." Tackle them roughly in priority order (P0 → P3), but they're independent — grab whichever fits the session.

The doc-readiness phase is essentially done; the **remaining open P2 items are RAM product code/template work**, not doc edits — and **OI-21, OI-22, OI-23 (plus OI-15's tail) all touch the same `SRS.yml` / `DocumentType` / `RequirementArtifactType` surface** and cross-reference each other. Treat them as **one coordinated change set** to do in a single pass when the user is ready to change the product, not four independent sessions.

**Last updated:** 2026-06-20

## How to use this file

- Check the box (`- [x]`) when an item is done; add a one-line note on what was decided/changed.
- **Owner tags:** ✍️ = I (Claude) can draft it, you confirm · 🧑 = needs your decision · 🔢 = needs a value/content only you have.
- New gaps we discover get appended with the next free `OI-n` (append-only IDs, like the BR/UC scheme).
- **Section numbers in older notes are pre-refactor.** Completed-item and change-log notes may cite numbered headings (e.g., "SRS §7.1", "Vision §4.2"); the spec docs are now unnumbered (name-based headings), so those `§` references are historical and may not resolve — they're left as-is to preserve the dated record.

---

## P0 — Implementation blockers (do first)

- [x] **OI-1 — Inline the data model (SRS §7.1).** ✅ *2026-06-04 — Inlined the corrected MVP `classDiagram` (Team / RequirementDocument / DocumentSection / DocumentSectionLock / RequirementArtifact / ArtifactLink / CommentThread / Comment + use-case decomposition) into §7.1 with prose + conceptual-model mapping; repointed §1.4; dropped the external mermaidchart link.*
- [x] **OI-2 — Resolve `traceability.md` FR wildcards.** ✅ *2026-06-04 — Removed every `FR-<area>*` wildcard. Per the project model (a use case is itself a high-level FR), the FR-IDs column now lists the SRS §5.2 cross-cutting subsystems each UC depends on (locking, autosave, validation, collaboration, export, AI, …) and `—` where the use case is self-contained; the universal FR-HIS-4 / FR-SEC-* are noted as applying everywhere. Updated the matrix's "How to use it" + footnotes accordingly.*

## P1 — Spec decisions that shape what gets built

- [x] **OI-3 — Commenting has no FR.** ✅ *2026-06-04 — Resolved by the methodology, not a new FR: per the use-cases-are-FRs model, UC-COL-2 (comment) and UC-COL-3 (resolve) **are** the functional requirements for commenting — their steps + Associated Information are the spec. The real fix was the policy: added **BR-20** (who may comment/resolve, and that commenting is allowed regardless of the review lock — for team Students and the Course Section's Instructor), resolved the UC-COL-2/UC-REV-2 contradiction, relaxed UC-COL-2 PRE-3, and added "status OPEN" to UC-COL-2's postcondition. (A short-lived `FR-CMT-1/2` was drafted then rolled back as duplicative.)*
- [x] **OI-4 — Version history has no UC.** ✅ *2026-06-05 — Decision: drop document version history from the MVP, deferred to a future release (not abandoned). `FR-HIS-1/2/3` (checkpoint, restore, retention) are retained in SRS §5.2.8 with their IDs but relabeled "Deferred — future release"; no use case is added (the gap is closed by removing the committed feature, not by adding a UC). `FR-HIS-4` (authorship metadata — creator/editor + timestamps) is **not** version history and stays in MVP scope. Vision describes version history as a post-MVP feature (consistent with OI-5/OI-6): §4.2.6 carries a post-MVP note, the §3.1 student stakeholder row lists it, and the §4.2 MVP exclusion list records it as deferred. §5.2.8 retitled "Authorship Metadata and Document Versioning Requirements" (TOC resynced).*
- [x] **OI-5 — Change-impact analysis + traceability matrix view.** ✅ *2026-06-05 — Decision: defer to a future release (post-MVP). The cluster covers the interactive traceability-matrix view, graph visualization (BO-6), highlighting of missing/incomplete linkages and orphan surfacing, and change-impact analysis — none had a UC or FR. The underlying typed links and trace navigation stay in MVP (UC-LNK-1..6; safe rename via UC-GLO-6 + FR-VAL-4). Vision updated: post-MVP notes added to §4.2.1, §4.2.4, and BO-6, and the capabilities added to the §4.2 MVP exclusion list. No UC/FR added; revisit when scoping a future release.*
- [x] **OI-6 — Broad smart-editing / auto-suggest.** ✅ *2026-06-05 — Decision: defer to a future release (post-MVP). Only glossary-term suggestion (`FR-GLO-3`) ships in the MVP; broader auto-suggestions (actors, requirement patterns, cross-document links) and auto-complete for requirement types/scenarios are deferred. Vision updated: post-MVP note added to §4.2.3, to-be process-flow step 6 reworded, and the capability added to the §4.2 MVP exclusion list. No new FR added.*
- [x] **OI-14 — Add the Requirement Link / graph layer to the §7.1 data model.** ✅ *2026-06-04 — Obviated by the corrected data model: `ArtifactLink` + `ArtifactLinkType` (and the outgoing/incoming relationships) are present in §7.1. The "graph-first" framing is consistent with the schema; no reconciliation needed.*
- [x] **OI-16 — Spec the whole-project cross-document review capability.** ✅ *2026-06-06 — Specced as a dedicated MVP use case. Added **UC-AI-10** (Request a whole-project review from the critique assistant): reviews **all** the team's requirement documents together (not hardcoded to five) for cross-document gaps/inconsistencies/conflicts/broken traceability; read-only; act on findings via UC-DOC-2/6 or UC-AI-8; reachable via a "Review whole project" action and routed from the project assistant (UC-AI-9, wired). Realized by new **FR-AI-21** (review behavior), **FR-AI-22** (include criteria in context), **FR-AI-23** (unavailable while criteria undefined). Per the user's added requirement, the review is governed by instructor/admin-configured **Cross-Document Review Criteria** — new glossary term + new **UC-CFG-4** to author them per course section; **BR-4** extended to make them instructor/admin-only; defining them is a precondition of UC-AI-10. Traceability rows added for UC-CFG-4 and UC-AI-10; TOCs regenerated; the scope note in `guides/cross-document-review-criteria.md` updated to point at the new UC/FRs (its Part 1/2 are now the default criteria + system prompt).*
- [x] **OI-17 — Define the artifact link-type compatibility matrix.** ✅ *2026-06-06 — Defined the matrix as an elaboration of BR-8 in `business-rules.md` (the single source of truth, chosen over UC-LNK-3 to co-locate with the rule it enforces). Rather than a 19×19×6 grid, it classifies requirement artifact types into four abstraction tiers (Tier 1 Business/Vision → Tier 4 Detailed/System) plus cross-cutting types (Stakeholder, Risk, Assumption, Dependency, Business Rule, Glossary Term, Background), then gives per-link-type source→target rules: DERIVES_FROM/REALIZES require source tier ≥ target tier (no high→low), REFERENCES is the any→any catch-all (only link allowed to touch Glossary Term/Background), IMPACTS from cross-cutting/constraining types → any requirement, MITIGATES → a Risk target, MOTIVATES from Stakeholder/Business Problem/Opportunity → any requirement. BR-8 amended to point to it; UC-LNK-3 (step 6) and UC-LNK-4 now reference the matrix instead of restating link types. Flagged dependency on OI-15(a): the type set (esp. Stakeholder/Dependency, which the §7.1 enum omits) must be revisited when OI-15 lands. (Surfaced 2026-06-04.)*

## P2 — Fill in committed values & finish partial use cases

- [x] **OI-20 — Settle the disposition of `guides/cross-document-review-criteria.md` (RAM product material, not spec-authoring guidance).** 🧑 *Surfaced 2026-06-10. This file is **RAM product material** — the default Cross-Document Review Criteria plus the runtime system prompt the RAM critique assistant uses when a **student team** asks for a whole-project review (UC-AI-10) of **their own** requirement set. It is NOT authoring guidance for the `docs/` specs, and NOT a tool for reviewing these specs; the resemblance is just RAM dogfooding its own document model. Decisions needed: (a) **Re-categorize** it in `CLAUDE.md`/`README.md` — it's currently filed under "supporting authoring/build guidance," which mis-frames it as doc-authoring guidance. (b) **Where should the default criteria set live** so it survives if this file is later removed? Candidate home is **UC-CFG-4** (which configures the criteria and whose step 2 already says "seeded from a system-provided default"), not UC-AI-10 (which only applies them). (c) **Spec-leak check before moving any content:** the guide's Part 1 is written in *this repo's* `docs/` idiom — it cites `§5.2`, append-only `FR-<AREA>-<n>` IDs, and the Course-Section/Document-Section overload. Decide whether RAM's **product** document templates actually impose these schemes on student docs (if yes, the criteria are legitimate product defaults; if no, genericize to product-level dimensions first). (d) **Stale pointer:** `CLAUDE.md` describes this file with "(see OI-16)", but OI-16 is closed — repoint to UC-AI-10 / FR-AI-21–23 if/when this is handled. (The earlier `spec-driven-setup/ → guides/` path in this file's OI-16 note and change log was corrected to the real location.) Folder kept temporarily; no doc surgery until this is decided.* **✅ Resolved 2026-06-10:** (a) **re-categorized** as RAM product material in `docs/CLAUDE.md`, `docs/README.md`, and root `CLAUDE.md`. (b) **Moved out of `guides/`** to a new `docs/product/` subtree; wired the spec to it — UC-CFG-4's step-2 system-provided default and UC-AI-10 now point at `docs/product/cross-document-review-criteria.md`. (c) **Genericized the spec-leak**, validated against the real product (`backend/src/main/resources/requirement-document-templates/*.yml`, `DocumentType`): student documents have **named, unnumbered** template sections and **system-assigned per-type keys** (`UC-1`, `FR-1`), so the criteria now name the SRS **Non-Use Case Functional Requirements** section instead of "§5.2" and drop the `UC-<AREA>-<n>` / "independent of section numbering" assumptions. (d) stale `(see OI-16)` pointer repointed to UC-AI-10 / FR-AI-21–23. (Also swept up two stray `*.tmp.*` files left in `.claude/commands/` and `requirement-document-templates/`.)
- [ ] **OI-21 — Adjust the RAM product's SRS template (`SRS.yml`) to match the structure of our RAM-requirements SRS.** 🧑 *Surfaced 2026-06-15 while relocating OE/CO/AS/DE into our RAM-requirements SRS (`docs/requirements/software-requirements-specification.md`). **Keep the two SRSs distinct:** (i) the **doc** we edit under `docs/` is the spec that describes the RAM product; (ii) the RAM **product's** built-in SRS template (`backend/src/main/resources/requirement-document-templates/SRS.yml`, surfaced via `DocumentType.SRS`) is what a student team fills in to author **their own** project's SRS. This item is about template (ii).*
  - ***Correction to an earlier premise:** the product SRS template is **not** missing constraint/operating-environment homes — `SRS.yml` already has `OPERATING_ENVIRONMENT` (RICH_TEXT), `DESIGN_CONSTRAINTS` (LIST), and `ASSUMPTIONS_DEPENDENCIES` (LIST). The authoring-home-table fix made the docs **match** the product, not the reverse.*
  - ***Concrete section delta to make `SRS.yml` resemble the docs SRS** (the template is a flat ordered list — "Overall Description" etc. are just ordering, not nesting):*
    - ***Add** sections: `PRODUCT_PERSPECTIVE` ("Product Perspective"), `USER_CLASSES` ("User Classes and Characteristics"), `DATA_DICTIONARY` ("Data Dictionary"), `REPORTS` ("Reports").*
    - ***Reorder** so Operating Environment / Design and Implementation Constraints / Assumptions and Dependencies sit right after the new User Classes section (the docs' "Overall Description" block), before Functional Requirements.*
  - ***Architecture decision — RESOLVED 2026-06-15: option (b).** Add an `ARCHITECTURAL_DESIGN` value to `DocumentType` and a new `ARCHITECTURAL_DESIGN.yml` template, then move `SOFTWARE_ARCHITECTURE`, `SYSTEM_CONTEXT`, `CONTAINER_DIAGRAM`, and `DEPLOYMENT` **out of** `SRS.yml` into it — so the product splits architecture into its own document the way the `docs/` set splits it into `design/architectural-design.md`. (Rejected option (a), keeping architecture inside the product SRS.) **To match the docs exactly, only those 4 architecture sections move.** OE/CO/AS/DE **stay in the product SRS** (`OPERATING_ENVIRONMENT`, `DESIGN_CONSTRAINTS`, `ASSUMPTIONS_DEPENDENCIES`) — that mirrors the docs, where `design/architectural-design.md` holds only System Context / Container / Deployment and the SRS owns OE/CO/AS/DE, and it keeps the authoring-home table's "`CONSTRAINT` → SRS" / "architecture-level `ASSUMPTION`/`DEPENDENCY` → SRS" routing valid.*
    - ***Update 2026-06-19 — the docs are now doctype-first with a single architecture-of-record.** (Supersedes a 2026-06-18 note that described a temporary two-level platform/module architecture split, since reversed.) There is **one** architecture doc, [`docs/design/architectural-design.md`](../design/architectural-design.md) — the complete arc42/C4 document (System Context / Container / core + RAM Component views / Architectural Conventions / Deployment). The product's single per-team `ARCHITECTURAL_DESIGN` document mirrors that complete architecture doc — a student team builds one system. The 4-section move (`SOFTWARE_ARCHITECTURE`, `SYSTEM_CONTEXT`, `CONTAINER_DIAGRAM`, `DEPLOYMENT` out of `SRS.yml`) stands as decided.*
  - ***Blast-radius note (check before editing `SRS.yml`):** `sectionKey`s flow through `DocumentTemplateRegistry`, `DocumentTemplate`, `DocumentService`, `DocumentSection`(+DTO/converter), and `DataInitializer` seed data. Dev uses `ddl-auto: create` (documents reseeded each restart), but verify no code path looks up a specific `sectionKey` (validation/ReqLint, AI context assembly) before removing or renaming any. **Do not edit the yml until the (a)/(b) decision is made.***
- [ ] **OI-19 — Align RAM entity audit metadata with FR-HIS-4.** ✍️ *Surfaced 2026-06-07 during source-code/entity review. The SRS requires creator/editor identity plus created/last-modified timestamps for every authored item. Current entity gaps to fix later: `RequirementDocument` has timestamps but no `createdBy`/`updatedBy`; `DocumentSection` has creator/editor fields but lacks `@CreatedBy`/`@LastModifiedBy`; `Comment` has an author but no last-editor field; `UseCaseMainStep`, `UseCaseExtension`, and `UseCaseExtensionStep` have no audit metadata. Decide whether `UseCase` itself relies on its paired `RequirementArtifact` audit record or gets explicit audit fields too.*
- [x] **OI-7 — SRS §9 quality-attribute thresholds.** ✅ *2026-06-06 — Committed by the user: USE-4 first-session success **95%**; PER-3 ReqLint **3s** for 95% of runs; PER-4 AI response **15s**; SCA-1 **~70 students (~75 total users)**, peak concurrency within PER-1's 100-concurrent envelope (kept as headroom above the real ~70 peak). All four `TBD` notes removed; §9 now carries no placeholders.*
- [x] **OI-8 — Incomplete use-case fields.** ✅ *2026-06-06 — Filled the three remaining TBD/TODO fields. UC-LNK-2 description: rationale is now "understand the type and direction of the relationship between the two connected artifacts and navigate from one to the other." UC-ART-1: completed the search-criteria table (Keyword / Artifact key / Artifact content / Type — data types, optionality, fuzzy/case-insensitive matching, team-scoped per BR-1/BR-2, glossary refs), set the display strategy (Artifact Key, Type, content excerpt, last-modified by/date) and sort (default ASC by Artifact Key, re-sortable by Type or last-modified). UC-DOC-2: replaced the per-document section-rules TBDs with a pointer — section set/order/rules are defined by each document's built-in Template (UC-TPL-1); full per-section enumeration stays with the template (UC-TPL-1), not duplicated here.* (The notification fields were resolved 2026-06-05: routine changes are not notified, FR-NOT-2; only the review workflow notifies, by email, FR-NOT-1.)
- [x] **OI-15 — Reconcile the implementation enums (§7.1) with the glossary and the doc set.** ✅ *2026-06-06 — Reconciled against the actual codebase enums (`team.projectpulse.ram`), with the user treating them as input, not frozen (open to code changes for consistency + completeness).*
  - **(a) `RequirementArtifactType`** — settled the canonical 22-type taxonomy and aligned the glossary "Requirement Artifact" list one-to-one with the SRS §7.1 enum. Decisions: **collapse `BUSINESS_RISK`/`RISK` → single umbrella `RISK`** (covers business/adoption, technical/feasibility, and security/safety risks via an optional category — chosen so `MITIGATES`/`IMPACTS` can target any risk or threat, not just business risk; Vision §2.5 renamed "Business Risks" → "Risks"); **add `DEPENDENCY`** as a tracked artifact (Vision §2.6); **keep** `STAKEHOLDER`, `PRECONDITION`, `POSTCONDITION`, `SUCCESS_METRIC`; **remove `background`** from the artifact list (it's prose, not an artifact); `OTHER` documented as an implementation fallback. **Codebase deltas for the user:** remove `BUSINESS_RISK` (keep `RISK`), add `DEPENDENCY` (migrate existing `BUSINESS_RISK` rows → `RISK`).
  - **(b) `DocumentType` / user stories** — keep `USER_STORY` (artifact) and `USER_STORIES` (document) as a **recognized-but-deferred, post-MVP** concept (no MVP document/template/UC), per the user (other instructors may want user stories; not implemented this MVP). Marked deferred in glossary + SRS §7.1.
  - **(c) Placement map** — added the canonical **artifact type → document/section** map to SRS §7.1 (MVP places by the section the Student is in, UC-ART-3; the map is the basis for the future add-by-type path).
  - **OI-17 consumer updated:** the BR-8 link-type matrix now targets a single umbrella `Risk` (MITIGATES/IMPACTS), keeps `Dependency` as a real type, drops `Background`, classifies preconditions/postconditions as Tier 3, and its OI-15 caveat is removed.
  - **Assumption/dependency key spaces unified (2026-06-06):** the two clashing per-document `AS-*` runs violated the graph-first rule that Artifact Keys are team-unique (BR-5). Fixed by making `AS-*`/`DE-*` single team-wide sequences: Vision §2.6 keeps business assumptions `AS-1`…`AS-5`; SRS §4.5 architecture assumptions renumbered `AS-1/2/3` → `AS-6/7/8`; dependencies stay `DE-1/2/3` (SRS only). The "local/independent" disambiguation note was replaced with the team-wide framing. Assumptions are still authored in both docs (business vs architecture), per the kept split.
- [ ] **OI-22 — Reconsider whether Operating Environment (`OE-*`) should be a first-class artifact type.** 🧑 *Surfaced 2026-06-15 while reviewing the SRS for consistency. The SRS defines `OE-1`…`OE-5` (Overall Description → Operating Environment), but unlike its sibling Overall-Description requirement spaces, OE has **no graph backing**: `RequirementArtifactType` (SRS Business Domain Model) has `CONSTRAINT`, `ASSUMPTION`, `DEPENDENCY` but **no `OPERATING_ENVIRONMENT`**, and the artifact type → authoring-home table has rows for `CONSTRAINT`/`ASSUMPTION`/`DEPENDENCY` but none for OE. So `CO-*`/`AS-*`/`DE-*` are typed graph artifacts while `OE-*` is prose-only.*
  - ***Prior state:** the asymmetry matched the RAM product template (`SRS.yml`), where `OPERATING_ENVIRONMENT` was `RICH_TEXT` (prose) while `DESIGN_CONSTRAINTS` and `ASSUMPTIONS_DEPENDENCIES` are `LIST` (typed artifacts).*
  - ***DECIDED 2026-06-15: promote `OPERATING_ENVIRONMENT` to a first-class artifact type** (reject keeping it as prose). The **spec-doc portion is done (2026-06-15)**; the **code + product template portion is deferred** — the user will modify `SRS.yml` and the enums later, not now. The coordinated change set:*
    - ***Spec docs — DONE 2026-06-15:** added `OPERATING_ENVIRONMENT` to the `RequirementArtifactType` enum and an authoring-home-table row (`OPERATING_ENVIRONMENT` → SRS → Operating Environment) in `software-requirements-specification.md`; added `operating environment` to the requirement-artifact list **and** a `## Operating Environment` definition entry in `project-glossary.md` (restoring the one-to-one glossary↔enum mapping); classified Operating Environment as Tier 4 (Detailed/system) in the BR-8 link-type matrix in `business-rules.md`.*
    - ***Backend** (`backend/.../ram/requirement/RequirementArtifactType.java`): add the `OPERATING_ENVIRONMENT` value (currently 22 values; → 23).*
    - ***Frontend** (`frontend/src/apis/ram/types.ts`): add `'OPERATING_ENVIRONMENT'` to the artifact-type list (kept in lockstep with the backend enum — both verified identical 2026-06-15).*
    - ***Product template** (`SRS.yml`): flip the `OPERATING_ENVIRONMENT` section from `RICH_TEXT` to `LIST`.*
    - ***Coordinate with **OI-15** (artifact-type taxonomy — this becomes the 23rd type) and **OI-21** (product SRS template structure — same `SRS.yml`/`DocumentType` surface). **Do not edit the enum/template until the user is ready to do the product change.****
- [ ] **OI-23 — Bind artifact type to its document section (enforce the authoring-home placement).** 🧑 *Surfaced 2026-06-15 while tracing how the RAM product fixes an artifact's type. **Finding: there is no `sectionKey → RequirementArtifactType` binding anywhere in the product.** `DocumentTemplate.SectionTemplate` (the `*.yml` model) carries only `sectionKey`/`title`/`type` (RICH_TEXT|LIST)/`guidance` — no artifact type; `DocumentSection` holds a `List<RequirementArtifact>` with no type constraint; `RequirementArtifactDto` supplies `type` as a separate `@NotNull` client field alongside `sourceSectionId`, and `RequirementArtifactService.saveRequirementArtifact` reads that client `type` (only to mint the key) without consulting the section. Consequences: (1) **UC-ART-3's "the section the Student is in fixes the artifact type" is not implemented** — type is free client input; (2) the SRS **artifact type → authoring-home table is documentation-only**, unenforced — nothing stops a `CONSTRAINT` being created in the Quality Attributes section.*
  - ***Options:** **(a)** *bind* — add an artifact-type field to `SectionTemplate`/`DocumentSection` and have the backend derive or validate the artifact's type from its section (faithful to UC-ART-3); **(b)** *validate only* — keep the client-supplied type but reject a type not allowed for that `sectionKey` via a server-side allow-map; **(c)** *soften the spec* — reword UC-ART-3 and the authoring-home table as advisory.*
  - ***Recommended direction (2026-06-15): option (a)** — it matches the section-fixes-type model already committed in UC-ART-3 and the home table, and it co-locates with the `SRS.yml` restructuring in **OI-21** (the per-section artifact type would be declared in the template yml alongside the OE/architecture changes). Not implementing now. Coordinate with **OI-21** (template surface) and **OI-22** (OE becomes a placeable type, so it needs a home/section binding too).*
- [x] **OI-9 — Multi-hop graph expansion (design).** ✅ *2026-06-06 — Decided: **lazy, cycle-safe, and unbounded in depth** (it's a graph — multi-hop traversal is not artificially capped). UC-LNK-1 (free exploration) defaults to one hop and lets the Student expand any displayed artifact one hop at a time on demand and keep going to any depth; the System lazy-loads each hop (responsive as the view grows) and never re-expands an already-shown artifact, so cycles terminate and the displayed set stays finite. Added extension 5a + Associated Information to UC-LNK-1 and cleared its open issue. UC-LNK-6 is the complementary **ordered, goal-driven** path along abstraction levels (Objective→Feature→…→FR) — also multi-level, lazy, cycle-safe; the LNK-1↔LNK-6 distinction is free exploration vs ordered path, not shallow vs deep. UC-LNK-2 (single-link details) is terminal: removed its mis-placed multi-hop open issue and pointed it at UC-LNK-1/UC-LNK-6. (Initial draft had a depth-3 cap on LNK-1; removed it — the lazy + cycle-safe properties already keep the view finite without limiting how far a student can trace.)*

- [ ] **OI-24 — Peer evaluation edits are not bounded by the submission window (code).** ✍️ *Surfaced 2026-06-19 during a docs review, reconciling UC-EVA-1 / BR-23 against the production code. `EvaluationService.updatePeerEvaluation` (`backend/.../evaluation/EvaluationService.java`) overwrites ratings and public/private comments with **no active-week / window guard** — only `addPeerEvaluation` checks that a submission is for the previous, active week. So a student can edit a past peer evaluation at any time, indefinitely; `PeerEvaluation` has no `completed`/`submitted`/`status` field and no lock. BR-23 was reworded this session to match that reality (evaluations stay editable; no finalize lock). **Decision needed:** if BR-24's "one week to complete it" is meant to bind edits too (not just creation), add a window check to `updatePeerEvaluation` (reject edits once the evaluatee's week is past the active submission window) and consider a finalize/lock model. Small code surface; affects grading integrity.*

## P3 — SRS completeness & housekeeping (before final delivery, not before code)

- [x] **OI-10 — SRS intro (§1.1–1.3).** ✅ *2026-06-06 — Filled, reference-first (no duplication, per the user). §1.1 Purpose of the Project: a meaty summary of what RAM is and who uses it, pointing to Vision and Scope (§3) for the full motivation rather than restating it. §1.2 Purpose of this Document: what the SRS covers (§4–§12) and the "link, don't restate" principle. §1.3 Document Conventions: the ID schemes (`FR-<AREA>-<n>`, `UC-<AREA>-<n>`, `BR-<n>`, per-type artifact keys, SRS-local `CO/OE/UI/SI/CI` + §9 quality codes), EARS/"shall" + use-case-is-an-FR note, Markdown-canonical, and the single-source-of-truth/reference convention. Also cleaned the leftover `[URL]` gdoc stub in §1.4.*
- [x] **OI-11 — SRS external interfaces (§8).** ✅ *2026-06-06 — Filled §8 from the §4 architecture, constraints (CO-1…CO-7), and the export/import/AI/notification FRs. §8.1 User Interfaces (UI-1…UI-3: Project Pulse SPA conventions, WCAG 2.1 AA, the two-column locking editor). §8.2 Software Interfaces: SI-1 external LLM Service via the server-side AI proxy (CO-6/SEC-4, context payload, JSON/structured output, credentials server-side, graceful degradation), SI-2 Project Pulse host platform (SSO identity/roles, course/team data reuse, relational-DB persistence, Gmail relay), SI-3 document export (PDF/DOCX/Markdown + bundle), SI-4 Project Source Material import (PDF/PPTX allowlist, text extraction). §8.3 API link text cleaned. §8.5 Communications: CI-1 review-workflow email via Gmail SMTP, CI-2 no email for routine changes (real-time instead), CI-3 HTTPS to the LLM service, CI-4 HTTPS browser↔server. All cited FRs/CO/UC IDs verified to resolve.*
- [x] **OI-12 — SRS §10 Deployment, §11 i18n, §12 Other.** ✅ *2026-06-06 — Filled, reference-first. §10 Deployment: RAM deploys as part of Project Pulse (CO-1/INT-1); the target environment specifics (Azure App Service, GitHub Actions CI/CD, OpenAI, load testing) are referenced to Vision §4.3, not restated; notes versioned DB migrations for the graph schema. §11 i18n: N/A for release 1.0 (TCU, U.S. English only; accessibility is a usability req, USE-1/§9.1, not i18n). §12 Other: no new requirements — references the cross-cutting concerns to where they already live (FERPA CO-5/SEC-3; SSO+RBAC FR-SEC-1..3; authorship FR-HIS-4; audit/soft-delete BR-12; install/startup via §10). All cited IDs verified. **Confirm:** the §11 "U.S. English only, no localization" assumption for release 1.0.*
- [x] **OI-13 — Cosmetic housekeeping.** ✅ *2026-06-06 — Replaced all 23 Frequency-of-Use `\*\*\* user` / `\*\*\* per milestone` template placeholders across the use cases with honest qualitative values ("Varies by team and project phase; most frequent during active authoring and near deadlines"; the two review use cases get "Around each project review milestone") — no fabricated per-week numbers. No `\*\*\*` residue remains in `use-cases.md`. The two remaining sub-items are **event-triggered, not open work**: Revision History tables are filled on version finalize (per CLAUDE.md, only when explicitly asked), and the `traceability.md` relocation note + `root-CLAUDE.md` path reference are reconciled if/when the docs are relocated into the project-pulse repo.*

- [x] **OI-18 — Apply the "no special formatting for glossary terms" convention doc-wide.** ✅ *2026-06-07 — Adopted and documented the two-register convention: glossary terms are plain lowercase prose by default, with Title Case reserved for headings, use-case titles, UI/table labels, document titles, code identifiers/enums, and artifact-type controlled labels in taxonomy/placement contexts. Added the convention to `CLAUDE.md` and `README.md`; applied it across the five core requirements docs.*

## Doc ↔ Code Gap Analysis (started 2026-06-20)

A first pass reconciling the requirements docs against the **actual codebase**. Project Pulse's core was built ~2 years ago and the specs (vision/scope, glossary, use cases, SRS, business rules) predate the architecture doc, so the code sometimes diverges from the spec — by a later design change, a simplification under time pressure, or a requirement that was never built. **Each item names the rule, what the code actually does (with `file:method`), and the divergence; the resolution direction is a decision — fix the code to match the doc, or amend the doc to match the (often better/simpler) code.** This is the user's call per item; nothing here is a defect to silently "fix."

*Scope of this pass:* the sharpest, most testable business rules (`BR-*`) and use-case claims, checked against the platform-core and built-RAM **service** layer. **Verified compliant (no gap):** BR-22 (WARs are allowed outside active weeks — `ActivityService.saveActivity` correctly imposes no active-week guard, while `EvaluationService.addPeerEvaluation` does), BR-25 (student peer-eval visibility — private comments are nulled for the student role in `EvaluationService.generateWeeklyPeerEvaluationSummaryForStudent` / `generateEvaluationSummariesForStudent`), BR-5 (per-team artifact-key uniqueness/stability — `RequirementArtifactService.generateNextArtifactKey` uses a `findForUpdate` per-(team,type) sequence). **Pass 1** covered the platform-core + built-RAM services against the sharpest BRs (OI-25..OI-31). **Pass 2 (2026-06-20)** covered comment access/authorship (BR-20), rubric/criterion, and account-lifecycle (delete vs deactivate) rules (OI-32..OI-34); it also confirmed the section service correctly enforces ≥1 instructor at the *section* level (the gap in OI-25 is team-level). **Pass 3 (2026-06-20)** covered the `user`/auth + registration gate, the email/notification flows, and the frontend router/guards (OI-35..OI-36). Findings: registration is invitation-gated (architecture TD-3 overstated, OI-35); notification gaps both ways (OI-36); the **frontend navigation guards are correct and consistent** with the documented auth/role-meta behavior (no divergence), and the **frontend RAM gaps** (no glossary-delete control, no standalone artifact browser, no review-status call, delete-team control commented out) are **already tracked in `traceability.md`** as 🟡/not-built rows, so they are not re-filed here. **Pass 4 (2026-06-20)** closed the remaining checks: the SRS Business Domain Model ↔ JPA entity field diff (entities **match** the model at the field level — no new structural gaps; the model is silent on audit/`version` fields, and the known entity gaps remain OI-19/OI-26/OI-32 and the enums OI-15/OI-22), the password-reset path (OI-39), the review-status enum (OI-37), and the frontend role hierarchy (OI-38, now resolved as a flat-check fragility rather than an unknown). **The four-pass gap analysis is now complete** for the backend services, the cross-cutting auth/notification machinery, the data model, and the frontend guards. The only deliberately-out-of-scope remainder is line-by-line review of individual Vue components/controllers/converters (the service layer, security, data model, and guards — where divergences concentrate — are covered). Related code-divergence items already logged: [OI-19] (audit metadata vs FR-HIS-4), [OI-23] (artifact-type→section binding), [OI-24] (peer-eval edit window).

- [ ] **OI-25 — A team can hold only ONE instructor; BR-21 expects ≥1 and "commonly two".** 🧑 *Surfaced 2026-06-20. BR-21: "Every team must be assigned at least one instructor; an instructor may be assigned to multiple teams. A team commonly has two instructors — a TCU instructor and the client." **Code reality:** `Team.instructor` is a single `@ManyToOne` field (`team/Team.java`); `TeamService.assignInstructorToTeam` (`team/TeamService.java`) **removes the current instructor before adding the new one**, so a team can never hold two — the "TCU instructor + client" pairing the rule describes is impossible. `transferTeamToAnotherSection` likewise assumes one instructor. Separately, **the ≥1-instructor minimum is unenforced**: `saveTeam` creates a team with no instructor and nothing requires one. **Decision:** (a) change `Team`→`Instructor` to `@ManyToMany` (or a join entity) to honor BR-21's two-instructor model, and add a ≥1 guard; or (b) amend BR-21 to the single-instructor reality if the client-as-instructor idea was dropped. Affects UC-INS-2/UC-INS-3 (assign/remove instructor), the transfer flow, and any "team instructors" UI. Data-model change → 🧑.*

- [ ] **OI-26 — Requirement-artifact / glossary-term deletion is a hard delete with no reference check and no soft delete (BR-12).** ✍️🧑 *Surfaced 2026-06-20. BR-12: "A glossary term or requirement artifact that is still referenced by another artifact may not be deleted; the references must be removed or repointed first. Deletion is logical (soft delete): deleted items leave active use but are retained for audit and are not reusable." **Code reality:** `RequirementArtifactService.deleteRequirementArtifact` (`ram/requirement/RequirementArtifactService.java`) does a plain `repository.delete(existing)` — (1) **no reference-integrity check** (an artifact that is the source/target of an `ArtifactLink`, or a glossary term referenced elsewhere, is deleted anyway, orphaning/breaking links), and (2) **no soft delete** (the row is physically removed; `RequirementArtifact` has no `deleted`/`active` flag, so there is no audit retention and the key could be considered reusable). This is the single delete path behind both UC-ART-6 and UC-GLO-7 (no glossary-specific delete exists). **Decision:** implement the reference-check + soft-delete model from BR-12, or relax BR-12 if hard delete is acceptable for the MVP. Coordinate with [OI-19] (audit fields).*

- [ ] **OI-27 — Artifact-link creation enforces none of BR-8 (self-link ban, type-compatibility matrix, uniqueness).** ✍️🧑 *Surfaced 2026-06-20. BR-8 (and its OI-17 link-type compatibility matrix): a link is unique on (source, target, type); an artifact may not link to itself; and a link type is permitted only between the artifact-type tiers it is defined for. **Code reality:** `ArtifactLinkService.createArtifactLink` (`ram/requirement/ArtifactLinkService.java`) sets type/source/target and saves — with **no self-link check, no (source,target,type) uniqueness check, and no compatibility-matrix validation**; it also looks up both artifacts by id **without verifying they belong to `teamId`** (a possible cross-team link if IDs are known). So the entire BR-8 matrix (carefully specified under OI-17) is documentation-only and unenforced — the same "spec is advisory, code doesn't enforce it" shape as [OI-23]. **Decision:** add server-side validation in `createArtifactLink` (self-link, uniqueness, tier matrix, team scoping), or down-scope BR-8 to whichever checks are wanted for the MVP.*

- [ ] **OI-28 — Review lock (BR-13/BR-14) is not enforced: a submitted document stays editable.** ✍️ *Surfaced 2026-06-20. BR-13: "On submission for review, a requirement document is locked for review and becomes read-only to all students on the team until an instructor returns it." **Code reality:** `DocumentService.updateRequirementDocument` (`ram/document/DocumentService.java`) only sets `status` (no transition validation), and `DocumentSectionService.updateDocumentSectionContent` / `UseCaseService.updateUseCase` check **only the section/use-case lock — never the parent document's `DocumentStatus`**. So while a document is `SUBMITTED`, a student who holds (or acquires) a section lock can still edit its content; the review read-only state exists as a status value but is not enforced on the edit path. Affects UC-REV-1/UC-REV-2. **Fix:** reject section/use-case content updates when the owning document's status is `SUBMITTED` (allow comments per BR-20). Mostly ✍️.*

- [ ] **OI-29 — Edit-lock auto-release timeout: code default is 15 minutes, BR-10 says 60 seconds.** 🧑 *Surfaced 2026-06-20. BR-10: "An edit lock held without activity longer than a configurable timeout (default 60 seconds) is released automatically." **Code reality:** `DocumentSectionService` and `UseCaseService` both inject `@Value("${ram.lock.default-lock-ttl:PT15M}")` — a **15-minute** default, not 60 seconds. Release is **lazy** (expired locks are cleared when someone next reads/locks/updates the destination), not via a background timer, and **content updates do not extend** the lock (only a re-lock by the same user calls `extend`), so "without activity" is approximated by a fixed TTL from acquisition. 60 s is almost certainly too aggressive for real editing and 15 min is the more usable choice → likely **amend BR-10** to the 15-minute default (and reword "without activity" to match TTL-from-acquisition, or add lock-extend on autosave). Small. 🧑 to pick the number/semantics.*

- [ ] **OI-30 — Per-team name uniqueness is unenforced for glossary terms (BR-6) and use cases (BR-7).** ✍️ *Surfaced 2026-06-20. BR-6: "A glossary term name is unique within a team's glossary." BR-7: "A use case name is unique within a team's Use Cases document." **Code reality:** `GlossaryService.saveGlossaryTerm` / `renameGlossaryTerm` (`ram/glossary/GlossaryService.java`) and `UseCaseService.saveUseCase` (`ram/usecase/UseCaseService.java`) perform no name-uniqueness check (no unique constraint or pre-save lookup), so duplicate term/use-case names within a team are accepted. **Fix:** add a team-scoped uniqueness check (and/or DB constraint) on create and rename. ✍️ (depends partly on the deferred ReqLint/terminology engine, but the hard uniqueness invariant is independent of it).*

- [ ] **OI-31 — Document "regenerate from template" (UC-TPL-1 / BR-3) is not implemented; create refuses if the type already exists.** 🧑 *Surfaced 2026-06-20. UC-TPL-1 is titled "regenerates team documents from built-in templates," and BR-3: "Only a course admin may create or regenerate a team's requirement documents. Regenerating documents that already contain authored content is destructive and requires an explicit, separate confirmation." **Code reality:** `DocumentService.createRequirementDocument` (`ram/document/DocumentService.java`) **throws `IllegalStateException` if a document of that type already exists** — i.e., it is create-once-per-type; there is no regenerate, no overwrite, and no destructive-confirmation flow. (Traceability marks UC-TPL-1 ✅, but only the create-if-absent half is built.) **Decision:** implement a confirmed destructive regenerate per BR-3, or rescope UC-TPL-1/BR-3 to "create once" and drop the regenerate language.*

### Pass 2 (2026-06-20) — comments, rubric/criterion, account lifecycle

- [ ] **OI-32 — Comment authorization and authorship are stubbed out (BR-20, FR-HIS-4).** ✍️🧑 *Surfaced 2026-06-20. BR-20 governs who may comment/resolve (team students + the course section's assigned instructor); FR-HIS-4 requires authorship metadata on every authored item. **Code reality:** in `CommentService` (`ram/collaboration/CommentService.java`) **every fine-grained authorization call is commented out** (`// authz.assertCanComment(...)`, `// authz.assertCanModerateThread(...)`, `// authz.assertCanEditComment(...)`) **and the author/creator is never set** (`// comment.setAuthor(user)`, `// thread.setCreatedBy(user)` are commented out in every create/add path). Consequences: (1) **comments persist with no author**, so UC-COL-2/UC-COL-3 cannot show who said what; and (2) `updateComment`/`deleteComment` have their author-ownership check commented out, so any team member can edit or delete **anyone's** comment. (Team scoping itself **is** enforced — every comment endpoint is gated by `teamMembershipAuthorizationManager` in `SecurityConfiguration`, lines ~261–273 — so BR-20's "any team student + the assigned instructor may comment/resolve" is satisfied; the missing piece is per-author edit/delete restriction and author attribution, not team membership.) This is the backend behind the 🟡 UC-COL-2/3 and extends [OI-19] (which only noted the missing last-editor field — here the author isn't populated at all). **Fix:** populate authorship (set author/createdBy) and add the author-ownership check on comment edit/delete; or, if comments are intentionally anonymous/team-editable for the MVP, amend BR-20 + FR-HIS-4 to say so.*

- [ ] **OI-33 — Account lifecycle: delete vs deactivate diverges from the use cases.** 🧑 *Surfaced 2026-06-20. The INS use cases retire an instructor by **deactivation** (UC-INS-6/UC-INS-7, the `enabled` flag) — there is **no "delete instructor" use case** — precisely so authored/graded history and team/section links survive. **Code reality:** `InstructorService.deleteInstructor` is exposed as `DELETE /instructors/{instructorId}` (`instructor/InstructorController.java`) and does a physical `deleteById`, which can break referential integrity (teams reference the instructor via the `@ManyToOne` of [OI-25]; authored RAM/comment content). Separately, `StudentService.updateStudent` lets any `ROLE_instructor` toggle a student's `enabled` flag, but there is **no "deactivate student" use case** (only UC-STU-4 delete, which the matching `deleteStudent` hard delete correctly realizes). **Decision:** (a) drop the delete-instructor endpoint in favor of deactivate-only, or add a delete-instructor use case if hard delete is genuinely intended; and (b) either add a deactivate-student use case or confirm the `enabled` toggle is an intentional undocumented admin control.*

- [ ] **OI-34 — Rubric/criterion management is largely undocumented (code-beyond-spec).** 🧑 *Surfaced 2026-06-20. The RUB area has a **single** use case — UC-RUB-1 (create a rubric) — but the code implements much more: rubric find/update/delete (`rubric/RubricService` + `RubricController`), **full criterion CRUD** (`rubric/CriterionService` + `CriterionController`: search/create/update/delete), add/remove-criterion-to-rubric (`RubricService`), and assign-rubric-to-section (`section/SectionService.assignRubric`). Since the rubric → criterion → rating model underpins peer evaluation (UC-EVA-*), this is a real spec gap — the inverse of the "specified but unbuilt" case: **built but unspecified.** **Decision:** backfill use cases for the existing rubric/criterion management (view/edit/delete rubric, criterion CRUD, assign rubric to a course section, add/remove criterion), or consciously record these as intentionally-undocumented admin utilities. (Also note: UC-RUB-1's actor is "Course Admin," consistent with the code's course-scoped access.)*

### Pass 3 (2026-06-20) — auth/registration, notifications, frontend

- [x] **OI-35 — Architecture doc overstates "open self-registration" (TD-3 / Security section); registration is invitation-gated.** ✍️ *Surfaced 2026-06-20. The architecture-of-record's Security section and risk **TD-3** state that `POST /students` and `POST /instructors` are `permitAll()` ⇒ "open self-registration, including instructor accounts." **Code reality:** the `permitAll()` is only the URL-filter rule (`SecurityConfiguration` lines 185, 194); both service paths first call `UserInvitationService.validateUserInvitation(...)` (`user/userinvitation/UserInvitationService.java`), which **throws `InvalidUserInvitationException`** unless a course-admin-issued invitation exists for that exact email **and** the token, role, course, and section all match. So a stranger cannot self-register — registration requires a valid invitation (UC-STU-1/UC-INS-1 issue the tokens). The residual risk is much smaller than TD-3 implies (e.g., the endpoint is reachable; an invitation is single-use and deleted after use). **Fix:** reword TD-3 and the Security "Known gaps" note to reflect the invitation gate — either downgrade/close TD-3 or restate the real residual concern — rather than claiming open registration. (Architecture-doc accuracy item; ✍️ once you confirm the intended framing.)* **✅ Resolved 2026-06-20:** reworded the Authentication "Known gaps" note in `design/architectural-design.md` to state registration is service-layer-gated by `validateUserInvitation` (not open), and rewrote **TD-3** (P1 → **P3**, "defense-in-depth") to frame the residual concern as "the sole gate is service-layer; the endpoint is unauthenticated/unthrottled — add a filter-chain rule and/or rate limiting (TD-6)."

- [ ] **OI-36 — Notification gaps, both directions (FR-NOT-*).** 🧑 *Surfaced 2026-06-20. (a) **Specified-but-unbuilt:** FR-NOT-1 requires an **email on review-workflow transitions** (document submitted / returned / accepted, via Gmail SMTP), but `DocumentController.updateDocumentStatus` only flips `DocumentStatus` with **no email send** (already flagged inline in `traceability.md` for UC-REV-1/UC-REV-2) — fine to leave unbuilt, but tracking it here as a first-class backlog item. (b) **Built-but-unspecified:** `system/WeeklyReminderScheduler` sends weekly **WAR / peer-evaluation submission-reminder emails** to students (per-section, on the configured due day, for reminder-eligible active weeks), yet **no FR-NOT or use case documents this behavior** — FR-NOT-1 is review-workflow only and FR-NOT-2 says routine changes are *not* notified. It supports BO-9/BO-10 (submission rate / time) but has no functional-requirement home. **Decision:** (a) build FR-NOT-1 review emails when the review workflow is finished, and (b) add an `FR-NOT-*` (and/or a use case) for the weekly reminder emails so the scheduler is spec'd, or consciously record it as an intentionally-undocumented background job.*

### Pass 4 (2026-06-20) — domain model, review-status, frontend role hierarchy, clock

- [ ] **OI-37 — The review workflow has no "accepted" state; `DocumentStatus` is `DRAFT/SUBMITTED/RETURNED` only.** 🧑 *Surfaced 2026-06-20. BR-14 ("an instructor may review, **accept**, or return") and UC-REV-2 (the instructor "accepts or returns") both treat **accept** as a first-class outcome, but `ram/document/DocumentStatus.java` defines only `DRAFT`, `SUBMITTED`, `RETURNED` — there is **no `ACCEPTED`/`APPROVED` value** — and the SRS Business Domain Model likewise lists only `DRAFT → SUBMITTED → RETURNED`. So an accepted document is indistinguishable from one still awaiting review (both stay `SUBMITTED`), and `updateDocumentStatus` can't record acceptance. **Decision:** add an `ACCEPTED` (or `APPROVED`) status to the enum **and** the SRS status list (and wire it into the review flow), or amend BR-14/UC-REV-2 to define "accept" as "leave `SUBMITTED` / not returned." Relates to [OI-28] (review-lock) and [OI-36] (review emails) — the review workflow is partial across all three.*

- [ ] **OI-38 — The frontend does not honor the `admin>instructor>student` role hierarchy.** ✍️🧑 *Surfaced 2026-06-20. `SecurityConfiguration`'s `RoleHierarchyImpl` (admin→instructor→student) only applies **server-side**; the JWT `authorities` claim is the user's **literal** `roles` string (`MyUserPrincipal.getAuthorities` → `JwtProvider`), and the frontend guard (`router/guards.ts` `checkPermissions`) does a **flat membership test** against that list with no hierarchy expansion. It works today only because admins are seeded with the multi-valued role string `"admin instructor"` (`DataInitializer`: b.wei, t.cook), so their `roleList` already contains `instructor`; a future admin created with `roles = "admin"` alone would be **locked out of `requiresPermissions: ['instructor']` routes on the frontend while still authorized server-side** — a silent client/server authorization mismatch. (Note also: instructors/admins lack `student` in their `roleList`, so `['student']`-only routes exclude them — intended for student-only actions, but it's by-convention, not by-hierarchy.) **Decision:** mirror the role hierarchy on the client (expand admin⇒instructor⇒student in the `roleList` or in `checkPermissions`), or accept the multi-valued-role convention and document it (and guarantee every admin's `roles` includes the lower levels). Low severity given current seed data; it's a fragility/consistency gap.*

- [ ] **OI-39 — Password-reset time logic bypasses the injected `Clock` (convention violation).** ✍️ *Surfaced 2026-06-20. `backend/CLAUDE.md` mandates: "All time-dependent code must inject the `Clock` bean and use `LocalDateTime.now(clock)`, never `LocalDateTime.now()`." But `ResetPasswordToken` sets `expiryDate = LocalDateTime.now().plusMinutes(5)` and `ResetPasswordService.validateForgetPasswordToken` compares against `LocalDateTime.now()` — both use the **real** system clock directly. (The 5-minute lifetime itself matches the architecture doc, so this is not a spec divergence — only a Clock-convention violation.) Functionally consistent in prod, but it bypasses the dev/test fixed clock (frozen at Aug 20 2023), so reset-token expiry can't be exercised through the test clock and behaves oddly under the dev profile. **Fix:** inject `Clock` into `ResetPasswordService`/token creation and use `LocalDateTime.now(clock)`. Small ✍️.*

---

## Change log

*(Chronological session notes. Completed `OI-n` items stay checked in place in their priority section above — keep the stable IDs there; this log records dated outcomes and work that never had an `OI-n`.)*

- **2026-06-04** — Authored `business-rules.md` (BR-1…BR-19) as the fifth core doc; wired CLAUDE.md / README / build command / root-CLAUDE.md.
- **2026-06-04** — Reconciled MVP assistant list (all but drafting); deferred dashboards/grading to post-MVP (review+comments only in MVP).
- **2026-06-04** — Filled SRS §9 Quality Attributes from committed facts (USE/PER/SEC/SAF/AVL/ROB/SCA/INT); added missing FR-COL-1.
- **2026-06-04** — Reverted glossary Artifact Key examples to the MVP's per-type running sequence (`FR-1`, `UC-1`).
- **2026-06-04** — **OI-1**: inlined the corrected MVP data model (Team/Document/Section/Lock/Artifact/Link/CommentThread/Comment + use-case decomposition) into SRS §7.1 with prose and conceptual-model mapping; repointed §1.4; removed the external mermaidchart link.
- **2026-06-04** — **OI-14** resolved: the corrected schema already includes the `ArtifactLink`/`ArtifactLinkType` graph layer. Opened **OI-15** (reconcile `RequirementArtifactType` enum vs glossary taxonomy); noted on OI-3 that comment entities now exist.
- **2026-06-04** — Wrote `guides/cross-document-review-criteria.md` (review methodology + critique-assistant system prompt for whole-project review). Opened **OI-16** to decide whether project-wide review becomes its own UC/FR.
- **2026-06-04** — **Clarified the use-cases-are-FRs model.** A use case *is* a high-level functional requirement; its "System shall" steps + Associated Information are its detailed spec. SRS §5.2 is reserved for non-use-case, system-level behaviors only. Initially drafted ~33 per-use-case CRUD FRs, then **rolled them back as duplicative** of the use cases (verifying each was covered first, folding the few missing details into the use cases: UC-ART-2 view fields, UC-LNK-5 deletion strategy, UC-COL-2 "status OPEN"). **Kept:** `FR-HIS-4` (cross-cutting authorship metadata), **BR-20** (comment access/lock policy) + the UC-COL-2/UC-REV-2 contradiction fix, the Markdown alignment in `FR-EXP-1`. Captured the principle in CLAUDE.md / README / build command / root-CLAUDE.md / feature.md. Decisions: artifacts edit without a lock; no BR for type→placement. Opened **OI-17** (link-type matrix); resolved **OI-3**.
- **2026-06-04** — **FR ID migration:** moved all SRS §5.2 FR IDs to the descriptive `FR-<AREA>-<n>` format (parallel to `UC-<AREA>-<n>`) — 278 replacements across 12 files. Resolved **OI-2** (traceability FR-IDs column now lists each UC's §5.2 cross-cutting dependencies, `—` where self-contained).
- **2026-06-05** — **Implementation-readiness audit** of all 32 active use cases (use cases + §5.2 vs FR coverage). Most are build-ready; remaining gaps logged. **Resolved notifications** (the biggest, cross-cutting gap): decided routine authoring changes are *not* notified (they propagate via real-time collaboration, new `FR-NOT-2`), and only the review workflow notifies — by email via Gmail SMTP (new `FR-NOT-1`, §5.2.13). Filled every notification field across GLO/DOC/ART/LNK/COL use cases accordingly; UC-REV-1/2 now specify email; added `FR-NOT` to the convention-doc area lists and traceability. Remaining audit gaps: UC-VAL-1 (ReqLint rule catalog), UC-TPL-1 (built-in template contents), plus the OI-8 TBDs, OI-9, OI-17.
- **2026-06-19** — **Docs review pass.** Reconciled UC-EVA-1 step 7 ↔ BR-23 against the production peer-eval code (evaluations are editable post-submission; reworded BR-23 to match, tightened step 7). Fixed UC-WAR-1's broken step-range numbering, removed the UC-INS-2 `TODO`, softened FR-AI-2 to zero-or-more findings, added UC-AI-10 to UC-CFG-2's governed-assistant list, and applied polish (spelling typos, `4a`/`6a` extension-label fixes in the report UCs, empty Frequency-of-Use fields in GLO/DOC UCs, and `Peer Evaluation Tool`→`Project Pulse` in the invitation-email templates). Opened **OI-24** (peer-eval edit window not enforced in code).
- **2026-06-20** — **Doc ↔ Code gap-analysis pass 1.** Reconciled the sharpest business rules + use-case claims against the platform-core and built-RAM service layer (code grounded, `file:method` cited). Opened **OI-25..OI-31**: team single-instructor vs BR-21 (OI-25), hard delete with no ref-check/soft-delete vs BR-12 (OI-26), unenforced BR-8 link rules (OI-27), unenforced review read-only lock BR-13/14 (OI-28), lock TTL 15 min vs BR-10's 60 s (OI-29), unenforced glossary/use-case name uniqueness BR-6/7 (OI-30), and "regenerate" not implemented vs UC-TPL-1/BR-3 (OI-31). Verified compliant: BR-22 (WARs outside active weeks), BR-25 (peer-eval visibility), BR-5 (artifact-key uniqueness). Not yet examined: BR-20 comments, rubric/criterion, section/student/instructor edge rules, frontend. Each item's resolution direction (fix code vs amend doc) left as a per-item decision.
- **2026-06-20** — **Doc ↔ Code gap-analysis pass 2.** Examined the pass-1 leftovers: comment service (BR-20/FR-HIS-4), rubric/criterion, and account-lifecycle rules. Opened **OI-32..OI-34**: comment authz + authorship stubbed out (OI-32), instructor hard-delete with no use case / student `enabled` toggle with no use case (OI-33), and undocumented rubric/criterion management — built-but-unspecified (OI-34). Confirmed section-level ≥1-instructor enforcement (vs the team-level gap in OI-25). Remaining for a later pass: the frontend, an exhaustive SRS-domain-model↔entity field diff, and the user/auth + notification flows.
- **2026-06-20** — **Doc ↔ Code gap-analysis pass 3.** Examined the user/auth + registration gate, email/notification flows, and frontend router/guards. Opened **OI-35** (architecture TD-3 overstates "open self-registration" — `validateUserInvitation` hard-gates it) and **OI-36** (notification gaps both ways: FR-NOT-1 review emails unbuilt; `WeeklyReminderScheduler` reminder emails unspecified). Corrected OI-32 (comment team-scoping *is* enforced via `teamMembershipAuthorizationManager`; the real gaps are author attribution + per-author edit/delete). Confirmed frontend guards are consistent with spec; frontend RAM gaps already live in `traceability.md`. Remaining: exhaustive domain-model↔entity field diff, password-reset edge cases, frontend role-hierarchy check.
- **2026-06-20** — **Doc ↔ Code gap-analysis pass 4 (final).** Closed the remaining small checks: SRS domain-model↔entity field diff (entities match; no new structural gaps), password-reset path, review-status enum, and frontend role hierarchy. Opened **OI-37** (no `ACCEPTED` `DocumentStatus` despite BR-14/UC-REV-2 "accept"), **OI-38** (frontend does a flat role check — no `admin>instructor>student` expansion; works only via the seeded `"admin instructor"` multi-role string), and **OI-39** (password-reset uses `LocalDateTime.now()` instead of the injected `Clock`, violating `backend/CLAUDE.md`). Four-pass analysis complete. Final tally: 17 code↔doc items (OI-19, OI-23, OI-24, OI-25..OI-39), of which OI-35 is resolved.
- **2026-06-05** — **UC-ART-3 made implementation-ready.** Placement model decided: MVP places an artifact in the **Document Section the Student is in** (the section sets its placement and type — no type field, no destination picker); the future requirements-graph "add" path picks the section or auto-places by type. Duplication = **advisory fuzzy warning** (same type + similar content), non-blocking; rewrote the 5a exception accordingly. Aligned UC-ART-5 (type fixed by section). Deferred the type→section placement *map* to **OI-15(c)** (needs the canonical type set).
